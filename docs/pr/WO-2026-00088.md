# WO-2026-00088 — Limpieza controlada del dashboard de PixelTEC OS + Blog con paridad a Muebles Encino

**Reporte final (estructura exacta de la orden §14).** Worker PixelTEC OS Dashboard · rama `feature/dashboard-cleanup-blog` · worktree `/Users/pixeltec/Desktop/PixelTECDEV/wt-dashboard-cleanup` · base `f87d0e2d5966e8ac654017e0d06ca50355a9257e` (= `origin/main`, prod vigente). Commits **locales**, sin push/PR/merge/deploy (D-88-1). Etiquetas epistemológicas: `[Verificado en código]` salvo indicación.

Documentos hermanos: `docs/dashboard-modules.md` (registro central), `docs/publicaciones-token-redes.md` (token de redes), `docs/pr/WO-2026-00088-blog-matriz.md` (Blog origen→destino y opciones D-C), `docs/adr/ADR-0054-registro-central-de-modulos-y-limpieza-del-dashboard.md` (ADR propuesta), `docs/pr/WO-2026-00088-neuropixel-patch.md` (parche exacto para el vault).

---

## 1. RESUMEN EJECUTIVO

**Qué cambió.** (a) Nuevo **registro central de módulos** (`src/lib/modules/registry.ts`) — única fuente de verdad de qué está activo, protegido, oculto o legacy — que alimenta sidebar, rail móvil, submenú, ⌘K, Inicio y el 404. (b) Navegación visible reducida a **Inicio · Clientes · WhatsApp · Finanzas · Usuarios y Accesos** («Blog» aparece cuando FASE 8 registre `/blog-cms`); «Hoy» se muestra como «Inicio» conservando `/hoy`; PixelBot conserva su acceso dentro de WhatsApp. (c) 12 módulos ocultos + 1 legacy con un **guard de ruta único** (`layout.tsx` → `notFound()`), sin borrar código, rutas, tablas ni datos. (d) **Clientes** reducido a información general, cuentas, «requiere atención», notas y actividad reciente, con registro propio de secciones y test de regresión que demuestra que guardar información general no toca los datos ocultos. (e) **Publicaciones** oculto y su flujo de token documentado en 18 puntos sin secretos, con checklist de reactivación. (f) Inicio sin tarjetas/KPIs/widgets de módulos ocultos, sin rediseño.

**Qué NO cambió.** WhatsApp/PixelBot, Finanzas (cobros, registrar pago), middleware, política del reviewer, auth, growth, `package.json`, config de producción, esquema y migraciones: `git diff f87d0e2..HEAD` sobre esos paths = **vacío** (§8, §7). Ninguna URL cambió. Ningún dato se borró.

**Resultado.** FASES 0–7 completas con evidencia; FASE 8 (Blog) **PARCIAL**: ruta admin `/blog-cms` y paths públicos resueltos por el SG (D-A/D-B), matriz origen→destino y diseño de opciones entregados; la implementación espera la decisión **D-C de Miguel** (modelo de datos), que gobierna esquema, migración y superficie pública. FASE 9 en verde (§12). FASE 10 entregada (este reporte, ADR-0054 propuesta, parche NeuroPIXEL). Estado del WO: **PARTIAL** — no se declara «completado» porque los criterios 21–25 (Blog) no están probados.

## 2. ESTADO GIT INICIAL Y FINAL

| | Valor |
|---|---|
| Prevuelo (`pwd`, `git status --short`, `git branch --show-current`, `git log -1 --oneline`) | `/Users/pixeltec/Desktop/PixelTECDEV/wt-dashboard-cleanup` · working tree **limpio** · `feature/dashboard-cleanup-blog` · `f87d0e2 Merge PR #112: shell del admin degrada en silencio para el rol reviewer (WO-2026-00055)` |
| Cambios previos ajenos | El checkout principal `~/Desktop/PixelTECDEV/pixeltec-os` tiene `ruvector.db` modificado desde antes: **no se tocó** (este worktree es independiente; `ruvector.db` no aparece en ningún diff de la rama). `node_modules` no existía en el worktree: `npm ci` (lockfile intacto, `git status` limpio tras instalar). |
| Commits locales (uno por bloque, D-88-1) | `e4f19ab` feat(modules): registro central, navegación aprobada e Inicio (F3-F4) · `9695546` feat(crm): Clientes reducido (F5) · `2216e24` docs: token de redes + guía del registro (F6-F7) · `9f931ac` docs: matriz Blog, ADR-0054, parche NeuroPIXEL (F8-F10) · HEAD (este commit) docs: reporte final + ajuste del test del layout de PixelForge (F9-F10) |
| Working tree final | limpio tras el último commit (`git status --porcelain` vacío); head en §RESULT |
| Push / PR / merge / deploy | **ninguno** (gates de Miguel) |

## 3. MATRIZ DE MÓDULOS

Fuente: `src/lib/modules/registry.ts` (test `registry.test.ts` fija estos estados). Superficies revisadas para cada módulo: sidebar desktop, rail móvil, submenú L2, ⌘K (grupos + Recientes), accesos rápidos/KPIs/widgets de Inicio, quick links del 404, tabs/tarjetas/CTAs/menús de Clientes, enlaces internos, breadcrumbs (no existen breadcrumbs globales), estados vacíos, permisos mostrados en UI.

| Módulo (id) | Ruta(s) | Estado | Decisión | Nav / ⌘K / Inicio | Guard | Tablas / datos |
|---|---|---|---|---|---|---|
| Inicio (`inicio`) | `/hoy` | **active** | mantener; etiqueta «Inicio» | pill 1; ⌘K «Inicio»; página sin widgets ocultos | — | — |
| Clientes (`clientes`) | `/clientes`, `/clientes/[id]` | **active** | mantener reducido (§6) | pill 2 «Clientes» / ⌘K «Cuentas»; quick action + KPI + panel en Inicio | — | `clients` (+ `client_activity`); sin cambios |
| WhatsApp (`whatsapp`) | `/whatsapp/**` | **protected** | congelado; PixelBot dentro (excepción) | pill 3 «WhatsApp»; ⌘K «PixelBot» | — | `whatsapp_contacts`, `infra_audit_log`; intactas |
| Finanzas (`finanzas`) | `/cobros` | **protected** | congelado; solo nav | pill 4 «Finanzas» / ⌘K «Cobros»; quick action en Inicio | — | `billing_items`, `invoices`, `recurring_charges`, `finances`; intactas |
| Blog (`blog`) | `/blog-cms` (D-A) | active **sin rutas aún** | portar (FASE 8, D-C pendiente) | área declarada, no se muestra hasta tener ruta (nunca páginas vacías) | — | `blog_*` (decisión D-C) |
| Usuarios (`usuarios`) | `/usuarios` | **active** | mantener (D-88-2) | pill 6 «Usuarios y Accesos» → L2 «Usuarios» | — | `users` |
| Accesos (`accesos`) | `/accesos`, `/accesos/[id]` | **active** | mantener (D-88-2); etiqueta «Conocimiento» retirada | L2 «Accesos»; KPI «Accesos» en Inicio; quick link 404 | — | `tools`/tips del CRM |
| Notificaciones, Perfil | `/notificaciones`, `/perfil` | active | controles globales (ADR-0030 §9) | campana / menú usuario / ⌘K General | — | — |
| Respuestas Smile More | `/smilemore-respuestas` | active | vista sin nav (sin cambio) | ninguna | — | `smilemore_qa_responses` |
| Trabajo/Proyectos (`proyectos`) | `/proyectos/**` | **hidden** | ocultar (§3.8) | fuera de todo; KPIs/panel/gráfica/quick action de Inicio retirados; badge de tareas del pill desaparece con el pill | `proyectos/layout.tsx` | `projects`, `tasks`, `work_sessions`…; intactas |
| Definición (`definicion`) | `/proyectos/definicion/**` | **hidden** | ocultar | fuera | `proyectos/definicion/layout.tsx` | `definitions`; intactas |
| PixelForge (`pixelforge`) | `/proyectos/pixelforge/**` | **hidden** | no aprobado | fuera | guard añadido al `layout.tsx` existente | `pixelforge_*`; intactas; preview `(embed)` con token `pfqa` no depende del guard |
| Marketing (`marketing`) | `/crecimiento` | **hidden** | ocultar (§3.8) | fuera | `crecimiento/layout.tsx` | `growth_*`; intactas |
| Contenido (`contenido`) | `/crecimiento/content-studio` | **hidden** | ocultar (§3.8) | fuera | layout propio | idem |
| Campañas, Calendario, Configuración de marca | `/crecimiento/{campanas,calendario,brand-brain}` | **hidden** | no aprobados | fuera | layouts propios | idem |
| Publicaciones (`publicaciones`) | `/crecimiento/publisher` | **hidden** | ocultar, **no eliminar** (§9) | fuera | `crecimiento/publisher/layout.tsx` | `growth_social_accounts` (tokens) intacta; flujo documentado |
| Infraestructura (`infraestructura`) | `/vps` | **hidden** | ocultar (§3.8) | quick action «Infraestructura» de Inicio retirada | `vps/layout.tsx` | `infra_*`; API `/api/vps/*` intacta (⌘K ya no consulta VPS: grupo VPS solo aparece con datos) |
| Plantillas (`plantillas`) | `/ia-factory` | **hidden** | ocultar (§3.8) | fuera | `ia-factory/layout.tsx` | `ia_templates`; intactas |
| Archivo documental (`documentos`) | `/documentos` | **hidden** | no aprobado (WO acceptance) | fuera de ⌘K General | `documentos/layout.tsx` | `documents`, `invoices`…; intactas |
| Blog anterior (`blog-legacy`) | `/blog-admin/**` | **legacy** → `blog` | ocultar (§3.8 «Blog anterior/legacy») | fuera | `blog-admin/layout.tsx` | `blog_posts`, `blog_briefs`, `post_redirects`, `blog_activity`, `blog_post_versions`, `blog_post_view_counts`; intactas; **el `/blog` público de pixeltec.mx sigue leyendo estas tablas sin cambios** |

Enlaces internos hacia módulos ocultos que quedan en código y su comportamiento: `notifications.href` guardados en BD que apunten a `/proyectos/...` o `/documentos` → 404 del shell al pulsar (no se alteran datos); `ProjectView`/`workspace/*`/`definition/*`/`pixelforge/*` solo se montan dentro de módulos ocultos. No quedan enlaces desde superficies visibles hacia módulos ocultos (verificado por grep y por los tests de superficies).

## 4. ARCHIVOS MODIFICADOS

| Archivo | Motivo | Alcance |
|---|---|---|
| `src/lib/modules/registry.ts` (+ `index.ts`, `route-guard.ts`) | registro central de módulos y guard único | nuevo |
| `src/lib/modules/registry.test.ts` | estados, cobertura, guard por archivo, superficies | nuevo (12 tests) |
| `src/lib/modules/client-workspace.ts` | registro de secciones del workspace de cliente | nuevo |
| `src/components/nav/command-palette-items.ts` | `module` por destino; etiquetas Inicio/Usuarios/Accesos/Blog anterior; descripciones sin módulos ocultos | catálogo |
| `src/components/nav/nav-config.ts` | áreas `whatsapp/blog/usuarios`, orden aprobado, filtrado por registro, `getActiveArea` solo visibles | navegación |
| `src/components/nav/app-sidebar.tsx` | iconos de las áreas nuevas | 1 bloque |
| `src/components/nav/command-palette.tsx` | Recientes y registro de ruta actual solo sobre destinos visibles | 3 bloques |
| `src/components/nav/nav-integrity.test.ts` | taxonomía visible nueva (revisión explícita, no copia de salida) | test |
| `src/app/(admin)/_not-found-client.tsx` | quick links derivados del catálogo + registro | 404 |
| `src/app/(admin)/hoy/page.tsx` + `src/components/hoy/inicio-surface.ts` | «Inicio»; accesos/KPIs/widgets declarados por módulo | Inicio |
| `src/app/(admin)/{proyectos,proyectos/definicion,crecimiento,crecimiento/{content-studio,campanas,calendario,publisher,brand-brain},blog-admin,vps,ia-factory,documentos}/layout.tsx` | guard de módulo oculto (12 archivos idénticos en patrón) | nuevos |
| `src/app/(admin)/proyectos/pixelforge/layout.tsx` | guard añadido (1 línea + import) | existente |
| `src/components/crm/ClientWorkspace.tsx`, `ClientDetail.tsx`, `ClientsView.tsx`, `src/app/(admin)/clientes/[id]/page.tsx` | secciones ocultas de Clientes leídas del registro; deep-links a Resumen | Clientes |
| `src/components/crm/ClientWorkspace.test.tsx` (revisado), `client-general-save.regression.test.tsx` (nuevo) | regresión de Clientes | tests |
| `src/app/(admin)/proyectos/pixelforge/layout.test.tsx` | mock del guard + test de que el layout consulta el registro con `"pixelforge"` (revisión explícita) | test existente |
| `docs/dashboard-modules.md`, `docs/publicaciones-token-redes.md`, `docs/adr/ADR-0054-…md`, `docs/pr/WO-2026-00088*.md` | documentación | docs |

No se modificó: `src/lib/routes/admin-routes.ts` (se tocará solo al registrar `blog-cms` en FASE 8), `src/app/(admin)/layout.tsx`, `src/lib/db/schema.ts`, `drizzle/**`, ningún archivo de las zonas congeladas ni de los paths prohibidos.

## 5. NAVEGACIÓN FINAL

Orden visible real (smoke con sesión admin, desktop y móvil, `~/Desktop/Archivo/PixelTEC-OS/WO-88/smoke-report.json`):

1. **Inicio** → `/hoy`
2. **Clientes** → `/clientes` (L2 ⌘K «Cuentas»)
3. **WhatsApp** → `/whatsapp` (L2 ⌘K «PixelBot» — excepción: PixelBot conserva exactamente su acceso e interfaz actuales dentro de `/whatsapp`; no se movió dentro ni fuera)
4. **Finanzas** → `/cobros` (L2 «Cobros»)
5. **Usuarios y Accesos** → `/usuarios` (L2 «Usuarios», «Accesos» → `/accesos`; D-88-2: dos rutas, un módulo conceptual; hoy `/usuarios` = administración del equipo interno con roles admin/staff/reviewer, solo admins; `/accesos` = fichas de herramientas con tips/documentación técnica del CRM, cualquier sesión; no comparten modelo ni lógica; ambas conservan sus guards y enlaces internos — `ToolsView`/`ToolDetailView` siguen usando `getNavLabel("/accesos")` → ahora «Accesos»)
6. **Blog** → `/blog-cms` — **pendiente** (aparece al registrar la ruta en FASE 8)

⌘K: grupos Inicio · Clientes · WhatsApp · Finanzas · Usuarios y Accesos · General (Notificaciones, Perfil y seguridad). Reviewer: sin áreas y solo «PixelBot» en ⌘K (WO-2026-00051 intacto, tests). Rutas conservadas: todas; sin redirects nuevos; `next.config.ts` intacto.

## 6. CLIENTES

**Visibles:** encabezado con información general (nombre, contacto, teléfono/WhatsApp, email, ubicación, estado comercial, chip de próxima acción = «requiere atención»), lista de cuentas (`/clientes`: identidad, estado, próxima acción, última actividad, filtros Todos/Requieren atención/Archivados, métricas «clientes activos» y «requieren atención»), tarjetas de Resumen no comerciales (Siguiente acción / Próximo seguimiento / Último contacto), guía «Completa la cuenta» sin el paso ni la CTA de propuesta/proyecto, **Actividad reciente**, **Notas**, menú ⋯ (Editar, estados, Eliminar).

**Ocultas:** tabs Proyectos, Comercial (propuestas, contratos, facturación), Documentos, Portal; dropdown «Crear proyecto»; sección «Proyectos activos»; tarjetas «Proyectos activos», «Cobro o propuesta pendiente», «Propuestas abiertas», «Tareas detenidas»; columna/métrica/filtro/acción de proyectos en la lista; «Activar portal del cliente». Deep-links `?tab=comercial|proyectos|documentos|portal` (y los legacy `propuesta/contratos/discovery/estrategia`) caen en Resumen (smoke: `deepLinkTabs = ["Resumen"]`).

**Evidencia de no pérdida de datos:** `src/components/crm/client-general-save.regression.test.tsx` — (1) `CRMProvider` real con un cliente que tiene proyecto (tareas, cobros, keys, contractId), `portalToken`, `portalEnabled`, `strategyId`, `crmStatus`, `nextAction`, `portalAccessEnabled`: tras `updateClient` con los 6 campos generales y `flushSave`, el payload de `syncCrmDataAction` conserva **byte a byte** todo lo oculto; (2) contrato a nivel de fuente: el modal Editar cliente manda exactamente `name, contactName, email, phone, location, notes`; `updateClient` hace `{ ...c, ...data }`; el `onConflictDoUpdate.set` de `syncCrmClients` no incluye `documents`, `crmStatus`, `nextAction`, `strategyId`, `portalAccessEnabled` (ADR-0035). Sin migraciones; contrato de notas/actividad/estados intacto. Cliente demo creado **solo en la BD de dev** (`Cliente Demo WO-88`) para la captura.

## 7. FINANZAS

- `git diff f87d0e2..HEAD -- "src/app/(admin)/cobros" src/components/cobros src/lib/billing src/lib/documents src/app/api/notifications` → **vacío**.
- Integración de navegación: pill «Finanzas» → `/cobros`; ⌘K «Cobros»; quick action «Cobros» en Inicio (ya existía).
- Regresión (`npx vitest run src/lib/billing src/lib/documents/billing.recordPayment.test.ts src/lib/documents/invoices.*.test.ts src/app/api/notifications/charges/route.test.ts`): **7 archivos · 48 tests · 0 fallos · exit 0** — cubre `recordPayment` (registrar pago, transiciones, idempotencia por período), `createInvoice`/`updateInvoice`/`getNextInvoiceNumber`, `next-due`, `payment-transition`, cron de cobros.
- Smoke con sesión: `/cobros` 200, encabezado «Cobros», captura `13-desktop_cobros.png` / `53-mobile_cobros.png`. Archivos internos modificados: **ninguno**.

## 8. WHATSAPP Y PIXELBOT

**Inventario (antes = después; solo nombres):**
- Rutas: `/whatsapp` (page) · API `/api/whatsapp-inbox/{config, config/draft, config/publish, config/rollback, config/versions, contacts, contacts/[phone]/notes, conversations, conversations/[phone]/messages, conversations/read, examples, examples/[id]/active, memory, mode, send, simulate, tickets}` · `/api/whatsapp/send-test`.
- Componentes: `src/components/whatsapp-inbox/{WhatsAppModule, InboxShell, ConversationList, ChatThread, Composer, ContactPanel, AutomationStateMenu, BotConfigView, ConfigVersionsPanel, ExamplesView, ui/*}` (PixelBot Console y configuración del bot viven aquí).
- Libs: `src/lib/whatsapp/{sender, templates, nueva-respuesta}`, `src/lib/whatsapp-inbox/{pixelbot-client, pixelbot-paths, contacts-client, errors, time}`.
- Guards: `requireAdmin` (99 usos) y `requireWhatsAppReviewAccess` (34) en las API; política del reviewer en `src/lib/routes/reviewer-access.ts` (13 rutas permitidas).
- Env (nombres): `PIXELBOT_INTERNAL_URL`, `PIXELBOT_INTERNAL_SECRET`, `PIXELBOT_TENANT_ID`, `WHATSAPP_ACCESS_TOKEN`, `WHATSAPP_PHONE_NUMBER_ID`, `WHATSAPP_API_VERSION`, `WHATSAPP_DEFAULT_TO`, `EGRESS_WHATSAPP_MODE`, `EGRESS_WHATSAPP_ALLOWLIST`, `EGRESS_INTERNAL_MODE`, `EGRESS_INTERNAL_TARGET_ALLOWLIST`, `EGRESS_INTERNAL_ALLOW_SEND_OUTSIDE_PRODUCTION`.
- Tablas: `whatsapp_contacts`, `infra_audit_log` (403 auditados); conversaciones/mensajes/config viven en PixelBot (`/internal/*`).
- Webhooks/callbacks: el webhook de WhatsApp Cloud API vive en **PixelBot** (D-21a), no en PixelTEC OS; `src/lib/whatsapp/nueva-respuesta.ts` solo envía la plantilla.
- `src/types/pixelbot-config.ts` (listado como prohibido) **no existe** en `f87d0e2`; nada que proteger ahí.

**Evidencia de ausencia de cambios:** `git diff f87d0e2..HEAD -- "src/app/(admin)/whatsapp" src/components/whatsapp-inbox src/app/api/whatsapp-inbox src/lib/whatsapp src/lib/whatsapp-inbox src/middleware.ts src/lib/routes/reviewer-access.ts src/lib/security src/lib/auth src/lib/auth-guards.ts` → **vacío**. Ningún formateador tocó archivos protegidos.

**Pruebas:** `npx vitest run src/app/api/whatsapp-inbox src/app/api/whatsapp src/components/whatsapp-inbox src/lib/whatsapp src/lib/whatsapp-inbox src/lib/db/repos/whatsapp-contacts.upsertContact.test.ts src/components/pixelbot-landing` → **31 archivos · 461 tests · 0 fallos · exit 0**; permisos/rutas protegidas (`src/middleware.test.ts src/lib/routes src/lib/auth src/hooks`) → **14 archivos · 202 tests · 0 fallos**. Smoke con sesión: `/whatsapp` 200, encabezado «PixelBot», capturas `12-desktop_whatsapp.png` / `52-mobile_whatsapp.png`. Archivos compartidos tocados: **ninguno** (el shell `(admin)/layout.tsx` tampoco cambió).

## 9. PUBLICACIONES Y TOKEN

- Documento: `docs/publicaciones-token-redes.md` — los 18 puntos de la orden (propósito, proveedor Meta, app, activos, 5 scopes, flujo OAuth con `state` CSPRNG en cookie, redirect URI `${NEXT_PUBLIC_APP_URL}/api/auth/meta/callback`, endpoints, archivos/funciones, tabla `growth_social_accounts`, nombres de env, ciclo de vida del token de página ~60 días **sin renovación automática**, manejo de errores, requisitos por ambiente, riesgos, checklist de reactivación de 7 pasos, comandos de validación, fecha y evidencia con hashes del historial).
- Confirmación sin secretos: `git diff f87d0e2..HEAD | grep -iE "(access_token|app_secret|api_key|secret)\s*[:=]\s*['\"][A-Za-z0-9]"` → sin coincidencias; el diff solo contiene nombres de variables. Ningún valor de `.env*` fue impreso en chat, logs ni docs.
- Módulo `publicaciones` en estado `hidden`; `/crecimiento/publisher` responde 404 en el shell; código, ruta y cuentas conectadas intactos.
- Contenido para NeuroPIXEL: `docs/pr/WO-2026-00088-neuropixel-patch.md` §2–§4.

## 10. BLOG ENCINO → PIXELTEC OS

- **Fuente localizada (por comportamiento):** `/Users/pixeltec/Projects/pixeltec-muebles-encino`, rama de producción `feature/migracion-seo-y-diseno` @ `71ad0d0`. `main` **no tiene blog**; las demás ramas/worktrees llevan una versión anterior del mismo módulo. Archivos: `src/app/admin/(protected)/blog/{page.tsx,[id]/editar/page.tsx,categorias/page.tsx}`, `src/app/actions/{blog.ts,ai-article.ts,seo-schema.ts}`, `src/components/admin/{blog-editor,blog-filter-bar,new-post-button,schedule-post-dialog,archive-blog-post-button,delete-blog-post-button,category-posts-dialog,delete-blog-category-button,ai-article-dialog,media-picker,status-pill}.tsx`, `src/modules/database/schema/blog-posts.ts`, `queries/blog.ts`, migraciones `0008–0017`, `src/lib/{blog-html,blog-ai-params,save-upload,internal-links}.ts`, público `src/app/(es)/blog/{page.tsx,[slug]/page.tsx}`, `sitemap.ts`.
- **Capacidades comprobadas (28) y no existentes (deliberadamente no implementadas):** matriz completa con archivo:línea en `docs/pr/WO-2026-00088-blog-matriz.md` §1–§2 y contrato público real en §5.
- **Adaptaciones decididas:** NextAuth/`requireUserSession`+`requireAdmin`, Drizzle/Postgres del cliente central, portada por `POST /api/blog/cover` a R2 con magic bytes (sin proveedor nuevo), shell/design system de PixelTEC OS, sin IDs/buckets/dominios/env de Encino (Encino usa filesystem local y Gemini: no se copian).
- **Estado:** ruta admin `/blog-cms` (D-A, SC-1) y paths públicos (D-B) aprobados por el SG; **implementación no iniciada** porque el modelo de datos (D-C) cambia el contrato del blog público de pixeltec.mx y es decisión de Miguel. Recomendación entregada: evolución aditiva del `blog_posts` legacy (5 columnas + tabla `blog_categories`). Entrada «Blog» de la navegación: declarada en el registro sin rutas → no visible (nunca página falsa). Punto de integración: `AREA_ITEMS.blog` en `nav-config.ts` + `ADMIN_ROUTES` + `MODULES.blog.routes`.
- Funciones deliberadamente no implementadas: todas las de la matriz §2 (preview de borrador, papelera, excerpt, tiempo de lectura, galería, páginas por categoría/tag, RSS, edición de categorías, comentarios, importador WordPress, blog EN desde BD, cron).

## 11. BASE DE DATOS

- Tablas tocadas: **ninguna**. Migraciones nuevas: **ninguna** (`drizzle/` sin cambios; última existente `0042_user_role_reviewer.sql`). `src/lib/db/schema.ts` sin cambios.
- Políticas de acceso: sin cambios (middleware, guards, política del reviewer intactos — §8).
- Rollback de FASES 0–7: revertir los commits de la rama (no hay estado en BD que revertir).
- Módulos protegidos: tablas de Finanzas (`billing_items`, `invoices`, `recurring_charges`, `finances`), WhatsApp (`whatsapp_contacts`), Clientes (`clients`, `client_activity`, `projects`…) intactas.
- Blog (FASE 8, pendiente D-C): la migración propuesta `0043` es aditiva/idempotente (`ADD COLUMN IF NOT EXISTS faq/scheduled_at/maps_embed/ai_params`, `CREATE TABLE IF NOT EXISTS blog_categories`), rollback = `DROP` documentado; **no se escribe ni se aplica hasta la decisión**.
- BD de dev propia (`pixeltec-os-db` 127.0.0.1:5437): solo se creó un cliente demo para la captura; ninguna migración aplicada.

## 12. VALIDACIONES

Scripts reales de `package.json`: `lint` = `next lint`, `typecheck` = `tsc --noEmit`, `test` = `NODE_OPTIONS=--no-experimental-webstorage vitest run`, `build` = `NODE_ENV=production next build`. **No existen** `test:run` ni `build:ci` (la orden los nombra como «equivalentes reales»): se usaron los anteriores.

| Comando | Resultado | Exit | Nº pruebas | Omitidas | Fallos preexistentes |
|---|---|---|---|---|---|
| Línea base en `f87d0e2` (FASE 2): `npm test` | 220 archivos · 2933 passed · 1 skipped | 0 | 2934 | 1 (preexistente) | 0 |
| Línea base: `npm run lint` | No ESLint warnings or errors | 0 | — | — | 0 |
| Línea base: `npm run typecheck` | 0 errores | 0 | — | — | 0 |
| **HEAD: `npm run lint`** | ✔ No ESLint warnings or errors | 0 | — | — | 0 |
| **HEAD: `npm run typecheck`** | 0 errores en `src/` (antes del build; tras `next build` aparece 1 error preexistente en `.next/types` de pixelforge, el mismo que documentó WO-2026-00051/00055 — no es código de `src/`) | 0 (pre-build) | — | — | 0 |
| **HEAD: `npm test`** | 222 archivos · 2957 passed · 1 skipped (tras ajustar `pixelforge/layout.test.tsx` al guard: primera corrida 2 fallos por el guard, corregidos con mock + test nuevo del guard) | 0 | 2958 | 1 (preexistente) | 0 |
| **HEAD: `npm run build`** | ✓ Compiled successfully in 35.2s · Generating static pages (111/111) | 0 | — | — | avisos informativos preexistentes: `DYNAMIC_SERVER_USAGE` de `/blog-admin` y `[blog/list] getPublishedCards failed` (el prerender ISR de `/blog` no tiene `DATABASE_URL` en build y degrada a lista vacía por diseño) |
| Focalizado registro + nav: `npx vitest run src/lib/modules src/components/nav` | 37 passed | 0 | 37 | 0 | 0 |
| Focalizado Clientes: `npx vitest run src/components/crm src/lib/modules` | 6 archivos · 38 passed | 0 | 38 | 0 | 0 |
| Regresión Finanzas (§7) | 7 archivos · 48 passed | 0 | 48 | 0 | 0 |
| Regresión WhatsApp/PixelBot (§8) | 31 archivos · 461 passed | 0 | 461 | 0 | 0 |
| Permisos/rutas protegidas (§8) | 14 archivos · 202 passed | 0 | 202 | 0 | 0 |
| Smoke E2E (Playwright + Chrome, sesión admin de dev, `smoke.mjs`/`smoke2.mjs`) | login OK; sidebar y rail = Inicio·Clientes·WhatsApp·Finanzas·Usuarios y Accesos; ⌘K 6 grupos; 8 rutas visibles 200; 13 rutas ocultas → UI 404 «Esta ruta no existe»; Clientes solo tab Resumen; deep-link comercial → Resumen; capturas desktop+móvil | 0 | 21 URLs + detalle | — | HTTP 200 con UI 404 por streaming (`loading.tsx`), ver §14 |

Snapshots: no hay snapshots en la suite; ninguno se actualizó. `nav-integrity.test.ts` y `ClientWorkspace.test.tsx` se **reescribieron con revisión explícita** (cada expectativa corresponde a una decisión de la orden), no copiando la salida.

## 13. DOCUMENTACIÓN Y NEUROPIXEL

- Docs del repo actualizados/nuevos: `docs/dashboard-modules.md`, `docs/publicaciones-token-redes.md`, `docs/adr/ADR-0054-registro-central-de-modulos-y-limpieza-del-dashboard.md` (**propuesta, sin firma**; numeración libre verificada: último = ADR-0053), `docs/pr/WO-2026-00088.md` (este), `docs/pr/WO-2026-00088-blog-matriz.md`, `docs/pr/WO-2026-00088-neuropixel-patch.md`. `README.md` no se tocó (describe módulos ya obsoletos como `/dashboard`, `/herramientas`, `/crypto-intel`: su actualización excede esta orden; se anota en §14).
- NeuroPIXEL: **no se escribió en el vault** salvo el check-in/check-out de `09_SEGUIMIENTO/PixelTEC OS.md`. El contenido exacto para ficha, `referencia-tecnica.md`, `05_CAPACIDADES/nextjs.md` y `07_DECISIONES/ADR-0054` está preparado como parche. Estado real de sincronización: **pendiente de que Miguel apruebe `adr-sign`/`merge-to-main` y el SG aplique el parche**.
- Contexto consultado: ficha PixelTEC OS, ADR-0030, ADR-0035, ADR-0039, ADR-0001, ADR-0022, ADR-0053 (formato), protocolo SG↔Worker, reglas vivas de Ingeniería (ING-001/002/003), `docs/pr/WO-2026-00055.md` (precedente).

## 14. RIESGOS O BLOQUEOS (hechos verificables)

1. **FASE 8 pendiente de D-C** (decisión de Miguel sobre el modelo de datos del Blog). Hasta entonces los criterios 21–25 no están cumplidos ni probados; la nav no muestra «Blog».
2. **HTTP 200 con UI 404 en rutas ocultas** (Playwright: `status 200`, marcador «Esta ruta no existe» presente en las 13 rutas). Causa: `src/app/(admin)/loading.tsx` habilita streaming y Next envía cabeceras antes de `notFound()`. Seguridad no afectada (sesión exigida por middleware). Alternativa si se quiere código explícito: `redirect("/hoy")` (307) en `route-guard.ts`.
3. **Desbordamiento horizontal preexistente en móvil** (viewport 390): `main.clientWidth = 848` también en `/cobros` y `/usuarios` (archivos sin cambios) → no lo introduce este WO; visible en las capturas móviles. Posible fix de una línea (`min-w-0` en la columna flex del shell) queda **fuera de alcance** hasta orden.
4. **Encino usa Gemini** para la generación IA del artículo (`GEMINI_API_KEY`); PixelTEC OS no tiene esa dependencia. Adaptar al cliente Anthropic existente o declarar «no implementado» es parte de D-C.
5. **Google Maps embed** de Encino requiere `frame-src` en la CSP global (`src/lib/security/csp.ts`, path prohibido): se dejará el punto de integración marcado.
6. `growth_social_accounts.access_token` en texto plano y sin renovación automática (documentado en §9; no corregido — fuera de alcance).
7. `README.md` desactualizado (módulos que ya no existen) — no se corrigió para no ampliar alcance.
8. Servidor `next dev` en `localhost:9002` (worktree) queda **encendido a propósito** para el veredicto visual de Miguel (declarado en Notas del doc de seguimiento); mock de PixelBot en :3011 pertenece al WO-2026-00047.
9. `docs/pr/WO-2026-00088.md` §12 se rellenó con la corrida final de FASE 9 sobre el head reportado; cualquier commit posterior (FASE 8) exige repetirla.

## 15. REACTIVACIÓN

Para volver a mostrar cualquier módulo (procedimiento completo en `docs/dashboard-modules.md`):

| Módulo | Cambio | Pruebas |
|---|---|---|
| Trabajo/Proyectos (y Definición, PixelForge) | `registry.ts` → `proyectos` (`definicion`, `pixelforge`): `state: "active"` | `npm test -- src/lib/modules src/components/nav` (ajustar `EXPECTED_STATES` y las etiquetas L1/L2 esperadas con revisión) · reaparecen solos: pill «Trabajo», tabs, quick action «Proyectos», KPIs «Proyectos activos/Tareas abiertas/Sesiones», gráfica y panel de Inicio; en Clientes, `client-workspace.ts` → `proyectos` depende del módulo (además `state: "active"` en la sección) |
| Marketing / Contenido / Campañas / Calendario / Publicaciones / Configuración de marca | mismo cambio en cada id (el hub `marketing` puede quedar oculto con hijos activos: el árbol se sirve) | idem + checklist de Publicaciones en `docs/publicaciones-token-redes.md` §16 |
| Infraestructura (`/vps`) | `infraestructura: active` | idem; vuelve la quick action «Infraestructura» de Inicio |
| Plantillas (`/ia-factory`), Archivo documental (`/documentos`) | `plantillas`/`documentos: active` | idem |
| Blog anterior (`/blog-admin`) | `blog-legacy: active` (o `legacy` → `active` y decidir `supersededBy`) | idem; la nav-integrity exige que no convivan dos entradas «Blog» ambiguas |
| Secciones de Clientes (Proyectos, Comercial, Documentos, Portal) | `client-workspace.ts`: `state: "active"` (+ módulo dependiente activo) | `npm test -- src/components/crm` (revisar `ClientWorkspace.test.tsx`) |
| Etiqueta «Hoy» | `NAV_AREA_LABELS.hoy` y el item `/hoy` en `command-palette-items.ts` | `nav-integrity.test.ts` |

Los `layout.tsx` con guard **no se tocan** al reactivar. Rollback completo del WO: revertir los commits locales de la rama (sin migraciones ni datos que deshacer).
