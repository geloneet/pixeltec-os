# WO-2026-00088 — Limpieza controlada del dashboard de PixelTEC OS + Blog con paridad a Muebles Encino

**Reporte final (estructura exacta de la orden §14).** Worker PixelTEC OS Dashboard · rama `feature/dashboard-cleanup-blog` · worktree `/Users/pixeltec/Desktop/PixelTECDEV/wt-dashboard-cleanup` · base `f87d0e2d5966e8ac654017e0d06ca50355a9257e` (= `origin/main`, prod vigente). Commits **locales**, sin push/PR/merge/deploy (D-88-1). Etiquetas epistemológicas: `[Verificado en código]` salvo indicación.

Documentos hermanos: `docs/dashboard-modules.md` (registro central), `docs/publicaciones-token-redes.md` (token de redes), `docs/pr/WO-2026-00088-blog-matriz.md` (Blog origen→destino y opciones D-C), `docs/adr/ADR-0054-registro-central-de-modulos-y-limpieza-del-dashboard.md` (ADR propuesta), `docs/pr/WO-2026-00088-neuropixel-patch.md` (parche exacto para el vault).

---

## 1. RESUMEN EJECUTIVO

**Qué cambió.** (a) Nuevo **registro central de módulos** (`src/lib/modules/registry.ts`) — única fuente de verdad de qué está activo, protegido, oculto o legacy — que alimenta sidebar, rail móvil, submenú, ⌘K, Inicio y el 404. (b) Navegación visible: **Inicio · Clientes · WhatsApp · Finanzas · Blog · Usuarios y Accesos**; «Hoy» se muestra como «Inicio» conservando `/hoy`; PixelBot conserva su acceso dentro de WhatsApp. (c) 12 módulos ocultos + 1 legacy con un **guard de ruta único** (`layout.tsx` → `notFound()`), sin borrar código, rutas, tablas ni datos. (d) **Clientes** reducido a información general, cuentas, «requiere atención», notas y actividad reciente, con registro propio de secciones y test de regresión que demuestra que guardar información general no toca los datos ocultos. (e) **Publicaciones** oculto y su flujo de token documentado en 18 puntos sin secretos, con checklist de reactivación. (f) Inicio sin tarjetas/KPIs/widgets de módulos ocultos, sin rediseño. (g) **Blog nuevo en `/blog-cms`** con paridad a las capacidades comprobadas de Muebles Encino (D-C Opción A aprobada por Miguel: evolución aditiva de la tabla `blog_posts` legacy — migración `0043` — + wizard IA sobre el cliente Anthropic ya existente, D-C-bis), verificado end-to-end en `localhost:9002`: crear → editar (Tiptap Markdown, tags, FAQ, portada e imágenes a R2, Google Maps embed) → publicar/programar/archivar/eliminar/revisar → superficie pública `/blog` con FAQ+FAQPage, tags, Maps, filtros y sitemap; excepción mínima de CSP para el host de Maps.

**Qué NO cambió.** WhatsApp/PixelBot, Finanzas (cobros, registrar pago), middleware, política del reviewer, auth, growth, `package.json`, config de producción, esquema y migraciones de otros módulos: `git diff f87d0e2..HEAD` sobre esos paths = **vacío** (§8, §7). Ninguna URL existente cambió. Ningún dato se borró. La única migración nueva (`0043`, Blog) es aditiva y solo se aplicó en la base de datos de desarrollo.

**Resultado.** FASES 0–10 completas con evidencia, incluida la FASE 8 (Blog) tras las decisiones D-A/D-B (Supervisor General) y D-C/D-C-bis/CSP-Maps (Miguel). FASE 9 en verde (§12): lint 0, tsc 0, 228 archivos / 3015 tests (+1 skipped preexistente), build 111 páginas → **112 páginas** con `/blog-cms` y sus subrutas. FASE 10 entregada (este reporte, ADR-0054 propuesta con sección Blog+CSP, parche NeuroPIXEL actualizado). Estado del WO: **DECLARADO, listo para verificación y los gates de Miguel** (visual-verdict final sobre el Blog, adr-sign, merge-to-main) — no se declara «completado» porque esos gates son autoridad exclusiva de Miguel/Supervisor General.

## 2. ESTADO GIT INICIAL Y FINAL

| | Valor |
|---|---|
| Prevuelo (`pwd`, `git status --short`, `git branch --show-current`, `git log -1 --oneline`) | `/Users/pixeltec/Desktop/PixelTECDEV/wt-dashboard-cleanup` · working tree **limpio** · `feature/dashboard-cleanup-blog` · `f87d0e2 Merge PR #112: shell del admin degrada en silencio para el rol reviewer (WO-2026-00055)` |
| Cambios previos ajenos | El checkout principal `~/Desktop/PixelTECDEV/pixeltec-os` tiene `ruvector.db` modificado desde antes: **no se tocó** (este worktree es independiente; `ruvector.db` no aparece en ningún diff de la rama). `node_modules` no existía en el worktree: `npm ci` (lockfile intacto, `git status` limpio tras instalar). |
| Commits locales (uno por bloque, D-88-1) | `e4f19ab` feat(modules): registro central, navegación aprobada e Inicio (F3-F4) · `9695546` feat(crm): Clientes reducido (F5) · `2216e24` docs: token de redes + guía del registro (F6-F7) · `9f931ac` docs: matriz Blog (diseño), ADR-0054 propuesta, parche NeuroPIXEL (F8-F10) · `6d0eca8` docs: reporte final + test del guard de PixelForge (F9-F10, validación pre-Blog) · HEAD (este commit) feat(blog): Blog en `/blog-cms` con paridad a Encino — D-C Opción A + D-C-bis + excepción CSP de Maps (F8, reasignación) |
| Working tree final | limpio tras el último commit (`git status --porcelain` vacío); head en §RESULT |
| Push / PR / merge / deploy | **ninguno** (gates de Miguel) |

## 3. MATRIZ DE MÓDULOS

Fuente: `src/lib/modules/registry.ts` (test `registry.test.ts` fija estos estados). Superficies revisadas para cada módulo: sidebar desktop, rail móvil, submenú L2, ⌘K (grupos + Recientes), accesos rápidos/KPIs/widgets de Inicio, quick links del 404, tabs/tarjetas/CTAs/menús de Clientes, enlaces internos, breadcrumbs (no existen breadcrumbs globales), estados vacíos, permisos mostrados en UI.

| Módulo (id) | Ruta(s) | Estado | Decisión | Nav / ⌘K / Inicio | Guard | Tablas / datos |
|---|---|---|---|---|---|---|
| Inicio (`inicio`) | `/hoy` | **active** | mantener; etiqueta «Inicio» | pill 1; ⌘K «Inicio»; página sin widgets ocultos | — | — |
| Clientes (`clientes`) | `/clientes`, `/clientes/[id]` | **active** | mantener reducido (§6) | pill 2 «Clientes» / ⌘K «Cuentas»; quick action + KPI + panel en Inicio | — | `clients` (+ `client_activity`); sin cambios |
| WhatsApp (`whatsapp`) | `/whatsapp/**` | **protected** | congelado; PixelBot dentro (excepción) | pill 3 «WhatsApp»; ⌘K «PixelBot» | — | `whatsapp_contacts`, `infra_audit_log`; intactas |
| Finanzas (`finanzas`) | `/cobros` | **protected** | congelado; solo nav | pill 4 «Finanzas» / ⌘K «Cobros»; quick action en Inicio | — | `billing_items`, `invoices`, `recurring_charges`, `finances`; intactas |
| Blog (`blog`) | `/blog-cms`, `/blog-cms/[id]/editar`, `/blog-cms/categorias` (D-A) | **active** | portado (FASE 8, D-C Opción A ejecutada) | pill 5 «Blog» / ⌘K «Blog» | — | `blog_posts` (+ 4 columnas aditivas), `blog_categories` (nueva); migración `0043` aplicada solo en BD dev |
| Usuarios (`usuarios`) | `/usuarios` | **active** | mantener (D-88-2) | pill 6 «Usuarios y Accesos» → L2 «Usuarios» | — | `users` |
| Accesos (`accesos`) | `/accesos`, `/accesos/[id]` | **active** | mantener (D-88-2); etiqueta «Conocimiento» retirada | L2 «Accesos»; KPI «Accesos» en Inicio; quick link 404 | — | `tools`/tips del CRM |
| Notificaciones, Perfil | `/notificaciones`, `/perfil` | active | controles globales (ADR-0030 §9) | campana / menú usuario / ⌘K General | — | — |
| Respuestas Smile More | `/smilemore-respuestas` | active | vista sin nav (sin cambio) | ninguna | — | `smilemore_qa_responses` |
| Trabajo/Proyectos (`proyectos`) | `/proyectos/**` | **hidden** | ocultar (§3.8) | fuera de todo; KPIs/panel/gráfica/quick action de Inicio retirados; badge de tareas del pill desaparece con el pill | `proyectos/layout.tsx` | `projects`, `tasks`, `work_sessions`…; intactas |
| Definición (`definicion`) | `/proyectos/definicion/**` | **hidden** | ocultar | fuera | `proyectos/definicion/layout.tsx` | `definitions`; intactas |
| PixelForge (`pixelforge`) | `/proyectos/pixelforge/**` | **hidden** | no aprobado | fuera | guard añadido al `layout.tsx` existente | `pixelforge_*`; intactas; preview `(embed)` con token `pfqa` no depende del guard |
| Marketing (`marketing`) | `/crecimiento` | **hidden** | ocultar (§3.8) | fuera | `crecimiento/layout.tsx` | `growth_*`; intactas |
| Contenido (`contenido`) | `/crecimiento/content-studio` | **hidden** | ocultar (§3.8) | fuera | layout propio | idem |
| Campañas, Calendario, Configuración de marca | `/crecimiento/{campanas,calendario,brand-brain}` | **hidden** | no aprobados | fuera | layouts propios | idem |
| Publicaciones (`publicaciones`) | `/crecimiento/publisher` | **hidden** | ocultar, **no eliminar** (§9) | fuera | `crecimiento/publisher/layout.tsx` | `growth_social_accounts` (tokens) intacta; flujo documentado |
| Infraestructura (`infraestructura`) | `/vps` | **hidden** | ocultar (§3.8) | quick action «Infraestructura» de Inicio retirada | `vps/layout.tsx` | `infra_*`; API `/api/vps/*` intacta (⌘K ya no consulta VPS: grupo VPS solo aparece con datos) |
| Plantillas (`plantillas`) | `/ia-factory` | **hidden** | ocultar (§3.8) | fuera | `ia-factory/layout.tsx` | `ia_templates`; intactas |
| Archivo documental (`documentos`) | `/documentos` | **hidden** | no aprobado (WO acceptance) | fuera de ⌘K General | `documentos/layout.tsx` | `documents`, `invoices`…; intactas |
| Blog anterior (`blog-legacy`) | `/blog-admin/**` | **legacy** → `blog` | ocultar (§3.8 «Blog anterior/legacy») | fuera | `blog-admin/layout.tsx` | `blog_posts`, `blog_briefs`, `post_redirects`, `blog_activity`, `blog_post_versions`, `blog_post_view_counts`; intactas; **el `/blog` público de pixeltec.mx sigue leyendo estas tablas sin cambios** |

Enlaces internos hacia módulos ocultos que quedan en código y su comportamiento: `notifications.href` guardados en BD que apunten a `/proyectos/...` o `/documentos` → 404 del shell al pulsar (no se alteran datos); `ProjectView`/`workspace/*`/`definition/*`/`pixelforge/*` solo se montan dentro de módulos ocultos. No quedan enlaces desde superficies visibles hacia módulos ocultos (verificado por grep y por los tests de superficies).

## 4. ARCHIVOS MODIFICADOS

| Archivo | Motivo | Alcance |
|---|---|---|
| `src/lib/modules/registry.ts` (+ `index.ts`, `route-guard.ts`) | registro central de módulos y guard único | nuevo |
| `src/lib/modules/registry.test.ts` | estados, cobertura, guard por archivo, superficies | nuevo (12 tests) |
| `src/lib/modules/client-workspace.ts` | registro de secciones del workspace de cliente | nuevo |
| `src/components/nav/command-palette-items.ts` | `module` por destino; etiquetas Inicio/Usuarios/Accesos/Blog anterior; descripciones sin módulos ocultos | catálogo |
| `src/components/nav/nav-config.ts` | áreas `whatsapp/blog/usuarios`, orden aprobado, filtrado por registro, `getActiveArea` solo visibles | navegación |
| `src/components/nav/app-sidebar.tsx` | iconos de las áreas nuevas | 1 bloque |
| `src/components/nav/command-palette.tsx` | Recientes y registro de ruta actual solo sobre destinos visibles | 3 bloques |
| `src/components/nav/nav-integrity.test.ts` | taxonomía visible nueva (revisión explícita, no copia de salida) | test |
| `src/app/(admin)/_not-found-client.tsx` | quick links derivados del catálogo + registro | 404 |
| `src/app/(admin)/hoy/page.tsx` + `src/components/hoy/inicio-surface.ts` | «Inicio»; accesos/KPIs/widgets declarados por módulo | Inicio |
| `src/app/(admin)/{proyectos,proyectos/definicion,crecimiento,crecimiento/{content-studio,campanas,calendario,publisher,brand-brain},blog-admin,vps,ia-factory,documentos}/layout.tsx` | guard de módulo oculto (12 archivos idénticos en patrón) | nuevos |
| `src/app/(admin)/proyectos/pixelforge/layout.tsx` | guard añadido (1 línea + import) | existente |
| `src/components/crm/ClientWorkspace.tsx`, `ClientDetail.tsx`, `ClientsView.tsx`, `src/app/(admin)/clientes/[id]/page.tsx` | secciones ocultas de Clientes leídas del registro; deep-links a Resumen | Clientes |
| `src/components/crm/ClientWorkspace.test.tsx` (revisado), `client-general-save.regression.test.tsx` (nuevo) | regresión de Clientes | tests |
| `src/app/(admin)/proyectos/pixelforge/layout.test.tsx` | mock del guard + test de que el layout consulta el registro con `"pixelforge"` (revisión explícita) | test existente |
| `docs/dashboard-modules.md`, `docs/publicaciones-token-redes.md`, `docs/adr/ADR-0054-…md`, `docs/pr/WO-2026-00088*.md` | documentación | docs |
| `drizzle/0043_blog_encino_parity.sql`, `src/lib/db/schema.ts` (`blogPosts` +4 columnas, tabla `blogCategories` nueva) | D-C Opción A: paridad aditiva con Encino | migración |
| `src/lib/blog/{types,queries/posts,public-post}.ts` (+ `public-post.test.ts` revisado) | `faq/scheduledAt/mapsEmbed/aiParams` en el modelo compartido; DTO público allowlist ampliada conscientemente (FAQ/tags/Maps SÍ cruzan; `aiParams` NO) | extendidos |
| `src/lib/blog-cms/{registry inexistente: paths,transitions,maps-embed,ai-params,schemas,queries,actions,ai}.ts` (+ 5 archivos `.test.ts`, 91 tests) | dominio y acciones del Blog nuevo sobre las tablas legacy | nuevo |
| `src/app/api/blog/image/route.ts` (+ test) | subida de imágenes del cuerpo a R2 (mismo endurecimiento que la portada) | nuevo |
| `src/app/(admin)/blog-cms/{page,[id]/editar/page,categorias/page}.tsx`, `src/components/blog/cms/*.tsx` | pantallas del Blog nuevo (paridad de UI con Encino) | nuevo |
| `src/components/blog/rich-markdown-editor.tsx` | acción «Párrafo» + subida de imagen (`onUploadImage`) | extendido |
| `src/app/blog/{page,blog-grid}.tsx`, `[slug]/{page,blog-post-client}.tsx`, `sitemap.ts` | FAQ+FAQPage, tags, Maps, filtros `?categoria/?etiqueta`, `nofollow`, barrido de programados | extendidos |
| `src/lib/security/csp.ts` (+ `csp.test.ts`) | excepción mínima de `frame-src` para el embed de Maps (Miguel, SC-2) | extendido |
| `src/lib/routes/admin-routes.ts`, `src/lib/modules/registry.ts`, `src/components/nav/{command-palette-items,nav-config,nav-integrity.test}.ts` | `blog-cms` en `ADMIN_ROUTES`; módulo `blog` con rutas; área «Blog» en posición 5 | extendidos |

No se modificó: `src/app/(admin)/layout.tsx`, ningún archivo de las zonas congeladas ni de los paths prohibidos. `src/lib/db/schema.ts` y `drizzle/**` se tocaron **exclusivamente** de forma aditiva para el Blog (fila de arriba); ninguna tabla de Finanzas/WhatsApp/PixelBot/Clientes cambió.

## 5. NAVEGACIÓN FINAL

Orden visible real (smoke con sesión admin, desktop y móvil, `~/Desktop/Archivo/PixelTEC-OS/WO-88/smoke-report.json`):

1. **Inicio** → `/hoy`
2. **Clientes** → `/clientes` (L2 ⌘K «Cuentas»)
3. **WhatsApp** → `/whatsapp` (L2 ⌘K «PixelBot» — excepción: PixelBot conserva exactamente su acceso e interfaz actuales dentro de `/whatsapp`; no se movió dentro ni fuera)
4. **Finanzas** → `/cobros` (L2 «Cobros»)
5. **Blog** → `/blog-cms` (L2 «Blog»; ⌘K «Blog»; también `/blog-cms/[id]/editar` y `/blog-cms/categorias`)
6. **Usuarios y Accesos** → `/usuarios` (L2 «Usuarios», «Accesos» → `/accesos`; D-88-2: dos rutas, un módulo conceptual; hoy `/usuarios` = administración del equipo interno con roles admin/staff/reviewer, solo admins; `/accesos` = fichas de herramientas con tips/documentación técnica del CRM, cualquier sesión; no comparten modelo ni lógica; ambas conservan sus guards y enlaces internos — `ToolsView`/`ToolDetailView` siguen usando `getNavLabel("/accesos")` → ahora «Accesos»)

⌘K: grupos Inicio · Clientes · WhatsApp · Finanzas · Blog · Usuarios y Accesos · General (Notificaciones, Perfil y seguridad). Reviewer: sin áreas y solo «PixelBot» en ⌘K (WO-2026-00051 intacto, tests). Rutas conservadas: todas; sin redirects nuevos; `next.config.ts` intacto. Smoke con sesión admin confirma el orden real en sidebar y rail móvil: `["Inicio","Clientes","WhatsApp","Finanzas","Blog","Usuarios y Accesos"]` (capturas `60`–`69` en `~/Desktop/Archivo/PixelTEC-OS/WO-88/`).

## 6. CLIENTES

**Visibles:** encabezado con información general (nombre, contacto, teléfono/WhatsApp, email, ubicación, estado comercial, chip de próxima acción = «requiere atención»), lista de cuentas (`/clientes`: identidad, estado, próxima acción, última actividad, filtros Todos/Requieren atención/Archivados, métricas «clientes activos» y «requieren atención»), tarjetas de Resumen no comerciales (Siguiente acción / Próximo seguimiento / Último contacto), guía «Completa la cuenta» sin el paso ni la CTA de propuesta/proyecto, **Actividad reciente**, **Notas**, menú ⋯ (Editar, estados, Eliminar).

**Ocultas:** tabs Proyectos, Comercial (propuestas, contratos, facturación), Documentos, Portal; dropdown «Crear proyecto»; sección «Proyectos activos»; tarjetas «Proyectos activos», «Cobro o propuesta pendiente», «Propuestas abiertas», «Tareas detenidas»; columna/métrica/filtro/acción de proyectos en la lista; «Activar portal del cliente». Deep-links `?tab=comercial|proyectos|documentos|portal` (y los legacy `propuesta/contratos/discovery/estrategia`) caen en Resumen (smoke: `deepLinkTabs = ["Resumen"]`).

**Evidencia de no pérdida de datos:** `src/components/crm/client-general-save.regression.test.tsx` — (1) `CRMProvider` real con un cliente que tiene proyecto (tareas, cobros, keys, contractId), `portalToken`, `portalEnabled`, `strategyId`, `crmStatus`, `nextAction`, `portalAccessEnabled`: tras `updateClient` con los 6 campos generales y `flushSave`, el payload de `syncCrmDataAction` conserva **byte a byte** todo lo oculto; (2) contrato a nivel de fuente: el modal Editar cliente manda exactamente `name, contactName, email, phone, location, notes`; `updateClient` hace `{ ...c, ...data }`; el `onConflictDoUpdate.set` de `syncCrmClients` no incluye `documents`, `crmStatus`, `nextAction`, `strategyId`, `portalAccessEnabled` (ADR-0035). Sin migraciones; contrato de notas/actividad/estados intacto. Cliente demo creado **solo en la BD de dev** (`Cliente Demo WO-88`) para la captura.

## 7. FINANZAS

- `git diff f87d0e2..HEAD -- "src/app/(admin)/cobros" src/components/cobros src/lib/billing src/lib/documents src/app/api/notifications` → **vacío**.
- Integración de navegación: pill «Finanzas» → `/cobros`; ⌘K «Cobros»; quick action «Cobros» en Inicio (ya existía).
- Regresión (`npx vitest run src/lib/billing src/lib/documents/billing.recordPayment.test.ts src/lib/documents/invoices.*.test.ts src/app/api/notifications/charges/route.test.ts`): **7 archivos · 48 tests · 0 fallos · exit 0** — cubre `recordPayment` (registrar pago, transiciones, idempotencia por período), `createInvoice`/`updateInvoice`/`getNextInvoiceNumber`, `next-due`, `payment-transition`, cron de cobros.
- Smoke con sesión: `/cobros` 200, encabezado «Cobros», captura `13-desktop_cobros.png` / `53-mobile_cobros.png`. Archivos internos modificados: **ninguno**.

## 8. WHATSAPP Y PIXELBOT

**Inventario (antes = después; solo nombres):**
- Rutas: `/whatsapp` (page) · API `/api/whatsapp-inbox/{config, config/draft, config/publish, config/rollback, config/versions, contacts, contacts/[phone]/notes, conversations, conversations/[phone]/messages, conversations/read, examples, examples/[id]/active, memory, mode, send, simulate, tickets}` · `/api/whatsapp/send-test`.
- Componentes: `src/components/whatsapp-inbox/{WhatsAppModule, InboxShell, ConversationList, ChatThread, Composer, ContactPanel, AutomationStateMenu, BotConfigView, ConfigVersionsPanel, ExamplesView, ui/*}` (PixelBot Console y configuración del bot viven aquí).
- Libs: `src/lib/whatsapp/{sender, templates, nueva-respuesta}`, `src/lib/whatsapp-inbox/{pixelbot-client, pixelbot-paths, contacts-client, errors, time}`.
- Guards: `requireAdmin` (99 usos) y `requireWhatsAppReviewAccess` (34) en las API; política del reviewer en `src/lib/routes/reviewer-access.ts` (13 rutas permitidas).
- Env (nombres): `PIXELBOT_INTERNAL_URL`, `PIXELBOT_INTERNAL_SECRET`, `PIXELBOT_TENANT_ID`, `WHATSAPP_ACCESS_TOKEN`, `WHATSAPP_PHONE_NUMBER_ID`, `WHATSAPP_API_VERSION`, `WHATSAPP_DEFAULT_TO`, `EGRESS_WHATSAPP_MODE`, `EGRESS_WHATSAPP_ALLOWLIST`, `EGRESS_INTERNAL_MODE`, `EGRESS_INTERNAL_TARGET_ALLOWLIST`, `EGRESS_INTERNAL_ALLOW_SEND_OUTSIDE_PRODUCTION`.
- Tablas: `whatsapp_contacts`, `infra_audit_log` (403 auditados); conversaciones/mensajes/config viven en PixelBot (`/internal/*`).
- Webhooks/callbacks: el webhook de WhatsApp Cloud API vive en **PixelBot** (D-21a), no en PixelTEC OS; `src/lib/whatsapp/nueva-respuesta.ts` solo envía la plantilla.
- `src/types/pixelbot-config.ts` (listado como prohibido) **no existe** en `f87d0e2`; nada que proteger ahí.

**Evidencia de ausencia de cambios:** `git diff f87d0e2..HEAD -- "src/app/(admin)/whatsapp" src/components/whatsapp-inbox src/app/api/whatsapp-inbox src/lib/whatsapp src/lib/whatsapp-inbox src/middleware.ts src/lib/routes/reviewer-access.ts src/lib/security src/lib/auth src/lib/auth-guards.ts` → **vacío**. Ningún formateador tocó archivos protegidos.

**Pruebas:** `npx vitest run src/app/api/whatsapp-inbox src/app/api/whatsapp src/components/whatsapp-inbox src/lib/whatsapp src/lib/whatsapp-inbox src/lib/db/repos/whatsapp-contacts.upsertContact.test.ts src/components/pixelbot-landing` → **31 archivos · 461 tests · 0 fallos · exit 0**; permisos/rutas protegidas (`src/middleware.test.ts src/lib/routes src/lib/auth src/hooks`) → **14 archivos · 202 tests · 0 fallos**. Smoke con sesión: `/whatsapp` 200, encabezado «PixelBot», capturas `12-desktop_whatsapp.png` / `52-mobile_whatsapp.png`. Archivos compartidos tocados: **ninguno** (el shell `(admin)/layout.tsx` tampoco cambió).

## 9. PUBLICACIONES Y TOKEN

- Documento: `docs/publicaciones-token-redes.md` — los 18 puntos de la orden (propósito, proveedor Meta, app, activos, 5 scopes, flujo OAuth con `state` CSPRNG en cookie, redirect URI `${NEXT_PUBLIC_APP_URL}/api/auth/meta/callback`, endpoints, archivos/funciones, tabla `growth_social_accounts`, nombres de env, ciclo de vida del token de página ~60 días **sin renovación automática**, manejo de errores, requisitos por ambiente, riesgos, checklist de reactivación de 7 pasos, comandos de validación, fecha y evidencia con hashes del historial).
- Confirmación sin secretos: `git diff f87d0e2..HEAD | grep -iE "(access_token|app_secret|api_key|secret)\s*[:=]\s*['\"][A-Za-z0-9]"` → sin coincidencias; el diff solo contiene nombres de variables. Ningún valor de `.env*` fue impreso en chat, logs ni docs.
- Módulo `publicaciones` en estado `hidden`; `/crecimiento/publisher` responde 404 en el shell; código, ruta y cuentas conectadas intactos.
- Contenido para NeuroPIXEL: `docs/pr/WO-2026-00088-neuropixel-patch.md` §2–§4.

## 10. BLOG ENCINO → PIXELTEC OS

- **Fuente localizada (por comportamiento):** `/Users/pixeltec/Projects/pixeltec-muebles-encino`, rama de producción `feature/migracion-seo-y-diseno` @ `71ad0d0`. `main` **no tiene blog**; las demás ramas/worktrees llevan una versión anterior del mismo módulo. Archivos: `src/app/admin/(protected)/blog/{page.tsx,[id]/editar/page.tsx,categorias/page.tsx}`, `src/app/actions/{blog.ts,ai-article.ts,seo-schema.ts}`, `src/components/admin/{blog-editor,blog-filter-bar,new-post-button,schedule-post-dialog,archive-blog-post-button,delete-blog-post-button,category-posts-dialog,delete-blog-category-button,ai-article-dialog,media-picker,status-pill}.tsx`, `src/modules/database/schema/blog-posts.ts`, `queries/blog.ts`, migraciones `0008–0017`, `src/lib/{blog-html,blog-ai-params,save-upload,internal-links}.ts`, público `src/app/(es)/blog/{page.tsx,[slug]/page.tsx}`, `sitemap.ts`.
- **Matriz origen→destino completa** (28 capacidades comprobadas con archivo:línea, funciones deliberadamente no implementadas, diferencias de modelo, contrato público real): `docs/pr/WO-2026-00088-blog-matriz.md` §1–§5.
- **D-C (modelo de datos) — APROBADA por Miguel: Opción A**, evolución aditiva. **D-C-bis — APROBADA:** wizard IA sobre el cliente Anthropic ya existente. **Ejecución completa documentada en la matriz §6.**
- **Adaptaciones aplicadas:** `requireUserSession` (leer/crear/guardar borrador/autosave) + `requireAdmin` (publicar/programar/archivar/restaurar/eliminar/categorías); Drizzle sobre `db` central (sin cliente paralelo); cuerpo en **Markdown** con el Tiptap ya existente (`RichMarkdownEditor`, extendido con acción «Párrafo» y subida de imagen) en vez del editor HTML propio de Encino; portada e imágenes del cuerpo a **R2** con magic bytes (`POST /api/blog/cover`, `POST /api/blog/image` nuevo) — Encino usa filesystem local, no se copió; wizard IA sobre `anthropicCreate` (`@/lib/ai/anthropic-egress`, ADR-0028) en vez de Gemini — sin `GEMINI_API_KEY` ni SDK nuevo; shell/design system de PixelTEC OS; sin IDs/buckets/dominios/env de Encino.
- **Base de datos:** MISMA tabla `blog_posts` del blog legacy — sin duplicar el modelo. Migración `drizzle/0043_blog_encino_parity.sql` (aditiva/idempotente): `faq jsonb`, `scheduled_at`, `maps_embed`, `ai_params`, índice `(status, scheduled_at)`, tabla nueva `blog_categories` (`name` único, `slug`, `parent_id` 1 nivel, `description`). Aplicada **solo en la BD de dev** (127.0.0.1:5437) con fila de control en `drizzle.__drizzle_migrations`; re-aplicada dos veces sin error (idempotencia verificada). **No aplicada en producción** (pendiente del gate `merge-to-main`/deploy de Miguel).
- **Entrada «Blog» en la navegación:** `src/lib/modules/registry.ts` (`blog.routes = ["/blog-cms"]`), `src/lib/routes/admin-routes.ts` (+ `blog-cms`), `AREA_ITEMS.blog` en `nav-config.ts` — pill 5 de 6, visible en sidebar/rail/⌘K (smoke §5).
- **Legacy claramente documentado:** `/blog-admin` (pipeline editorial con briefs/gate de revisión humana/stages) queda **`legacy`** (oculto, no eliminado — §3); comparte la misma tabla `blog_posts`, así que cualquier post creado en `/blog-cms` es visible/editable también desde el código legacy si se reactivara — no hay migración de datos entre modelos porque es el mismo modelo.
- **Excepción de CSP (Miguel, SC-2):** `src/lib/security/csp.ts` — `frame-src` gana únicamente `https://www.google.com` para el embed de Maps; `csp.test.ts` fija que ninguna otra directiva ni ruta protegida cambia. Verificado en navegador: **cero errores de CSP en consola** al cargar un artículo con Maps embebido (Playwright, filtro `content security policy`). El `<iframe>` no llegó a pintar contenido real porque este entorno de desarrollo no tiene salida de red hacia `google.com` (confirmado también con `curl` directo desde la terminal: HTTP 400) — limitación de red del sandbox, no del código ni de la CSP.
- **Validación end-to-end en `localhost:9002`** (sesión admin real, Playwright + Chrome) — evidencia literal (`~/Desktop/Archivo/PixelTEC-OS/WO-88/smoke-blog-report.json` + capturas `60`–`69`):
  - `GET /blog-cms` sin sesión → **307** a `/login?redirect=%2Fblog-cms` (admin exige sesión).
  - Crear (Manual) → editor en `/blog-cms/<id>/editar`; autosave confirmado (`✓ Guardado a las 07:38 p.m.`).
  - Publicar (admin) → `GET /blog/<slug>` → **200**; HTML contiene `<title>` con el título real, `rel="canonical" href="https://pixeltec.mx/blog/<slug>"`, `meta name="description"` con el texto exacto guardado, JSON-LD `"@type":"BlogPosting"` y `"@type":"FAQPage"`, `<dl>` con la pregunta/respuesta de FAQ, `<iframe src="https://www.google.com/maps/embed…">`, pills de las 3 etiquetas.
  - `GET /sitemap.xml` → contiene la entrada `/blog/<slug>` del post publicado.
  - Volver a **Borrador** → `GET /blog/<slug>` → **404**.
  - **Programar** (+5 min) → `GET /blog/<slug>` → **404** (ni borrador ni programado exponen la URL pública — criterio 24 cumplido).
  - Categoría nueva creada en `/blog-cms/categorias` y visible en el listado.
- **Pruebas automatizadas (91 nuevas, todas en verde):** `src/lib/blog-cms/{transitions,maps-embed,ai-params}.test.ts` (23, puras: transición por intención incl. `scheduled`, validación del host de Maps, tonos IA) · `src/lib/blog-cms/queries.test.ts` (9: throttle del barrido de 60 s, `force`, dedupe de slug `-2…`, protección contra loop >50) · `src/lib/blog-cms/actions.test.ts` (22: permisos backend por acción — publicar/programar/archivar/eliminar/categorías exigen admin; leer/crear/guardar-borrador exigen solo sesión —, validación zod, gate de integridad al publicar, categoría nueva) · `src/app/api/blog/image/route.test.ts` (6: admin-only, sin SVG, 5 MB, magic bytes) · `src/lib/security/csp.test.ts` (4 nuevos: excepción exacta, sin comodines, resto de la política intacta, misma excepción en `/whatsapp`/`/cobros`/`/blog`) · `src/lib/blog/public-post.test.ts` (revisado con evidencia: `faq`/`tags`/`mapsEmbed` SÍ cruzan al DTO público porque la página los renderiza; `aiParams` NO cruza, mismo criterio que `briefSource`).
- Funciones deliberadamente no implementadas (matriz §2, sin cambios): preview de borrador, papelera, excerpt, tiempo de lectura, galería, páginas públicas por categoría/tag, RSS, edición de categorías, comentarios, importador WordPress, blog EN desde BD, cron; además la pestaña «Snippets» del editor de Encino (depende de un módulo SEO Control Center ausente en PixelTEC OS).

## 11. BASE DE DATOS

- **Tablas tocadas (aditivo, solo Blog):** `blog_posts` gana 4 columnas (`faq`, `scheduled_at`, `maps_embed`, `ai_params`) + 1 índice; tabla nueva `blog_categories`. Ninguna otra tabla del producto cambió: `schema.ts` sin diferencias fuera de la sección Blog (`git diff` acotado a ese bloque).
- **Migración nueva:** `drizzle/0043_blog_encino_parity.sql` (aditiva, idempotente — `ADD COLUMN IF NOT EXISTS`, `CREATE TABLE IF NOT EXISTS`, FKs en `DO $$ … IF NOT EXISTS`). Generada con `drizzle-kit generate` y endurecida a mano con los `IF NOT EXISTS`. **Aplicada en la BD de dev** (`pixeltec-os-db`, 127.0.0.1:5437) el 2026-08-25 vía `docker exec … psql`, con fila de control insertada en `drizzle.__drizzle_migrations` (mismo procedimiento que `0042`); re-ejecutada dos veces sin error, confirmando idempotencia. **NO aplicada en producción** — pendiente del gate `merge-to-main`/deploy de Miguel; el archivo trae el mismo procedimiento de aplicación gobernada en su cabecera.
- **Rollback documentado** (cabecera de `0043_blog_encino_parity.sql`, no ejecutado): `DROP INDEX`/`ALTER TABLE … DROP COLUMN` de las 4 columnas + `DROP TABLE blog_categories`; si algún post quedó `status='scheduled'` con código anterior a esta migración, degradarlo primero a `draft` (el código legacy no reconoce ese estado).
- **Políticas de acceso:** sin cambios en middleware, guards ni política del reviewer (§8). Las nuevas Server Actions del Blog reutilizan `requireUserSession`/`requireAdmin` existentes; ninguna tabla nueva tiene RLS propia de Postgres (el producto no usa RLS — cliente centralizado, ver S-1).
- **Módulos protegidos:** tablas de Finanzas (`billing_items`, `invoices`, `recurring_charges`, `finances`), WhatsApp (`whatsapp_contacts`), Clientes (`clients`, `client_activity`, `projects`…) intactas — ningún cambio de esquema fuera de Blog.
- **BD de dev** (`pixeltec-os-db` 127.0.0.1:5437): además de la migración, se creó un cliente demo (`Cliente Demo WO-88`) y, durante el smoke del Blog, 3 entradas de prueba (publicada/borrador/programada, título «Cómo elegir el stack correcto para tu PyME en 2026») y una categoría demo («Automatización WO-88») — quedan en dev como evidencia visual para Miguel, no se tocó BD de producción en ningún momento.

## 12. VALIDACIONES

Scripts reales de `package.json`: `lint` = `next lint`, `typecheck` = `tsc --noEmit`, `test` = `NODE_OPTIONS=--no-experimental-webstorage vitest run`, `build` = `NODE_ENV=production next build`. **No existen** `test:run` ni `build:ci` (la orden los nombra como «equivalentes reales»): se usaron los anteriores.

| Comando | Resultado | Exit | Nº pruebas | Omitidas | Fallos preexistentes |
|---|---|---|---|---|---|
| Línea base en `f87d0e2` (FASE 2): `npm test` | 220 archivos · 2933 passed · 1 skipped | 0 | 2934 | 1 (preexistente) | 0 |
| Línea base: `npm run lint` | No ESLint warnings or errors | 0 | — | — | 0 |
| Línea base: `npm run typecheck` | 0 errores | 0 | — | — | 0 |
| **HEAD: `npm run lint`** | ✔ No ESLint warnings or errors | 0 | — | — | 0 |
| **HEAD (post-Blog): `npm run typecheck`** | 0 errores en `src/` (`tsc --noEmit` puro, antes de build) | 0 | — | — | 1 preexistente en `.next/types` tras `next build` (pixelforge, mismo de WO-51/55) |
| **HEAD (post-Blog): `npm test`** | 228 archivos · 3015 passed · 1 skipped | 0 | 3016 | 1 (preexistente) | 0 |
| **HEAD (post-Blog): `npm run build`** | ✓ Compiled successfully · Generating static pages (**112/112**, +1 por `/blog-cms`) | 0 | — | — | mismos avisos informativos preexistentes de `/blog-admin` y `getPublishedCards`; sin avisos nuevos de `/blog-cms` |
| Focalizado registro + nav: `npx vitest run src/lib/modules src/components/nav` | 37 passed | 0 | 37 | 0 | 0 |
| Focalizado Clientes: `npx vitest run src/components/crm src/lib/modules` | 6 archivos · 38 passed | 0 | 38 | 0 | 0 |
| Regresión Finanzas (§7) | 7 archivos · 48 passed | 0 | 48 | 0 | 0 |
| Regresión WhatsApp/PixelBot (§8) | 31 archivos · 461 passed | 0 | 461 | 0 | 0 |
| Permisos/rutas protegidas (§8) | 14 archivos · 202 passed | 0 | 202 | 0 | 0 |
| **Blog (§10, nuevo):** `npx vitest run src/lib/blog-cms src/app/api/blog src/lib/security/csp.test.ts src/lib/blog/public-post.test.ts` | 12 archivos · 91 passed | 0 | 91 | 0 | 0 |
| Smoke E2E dashboard (Playwright + Chrome, sesión admin de dev, `smoke.mjs`/`smoke2.mjs`) | login OK; sidebar y rail = Inicio·Clientes·WhatsApp·Finanzas·Blog·Usuarios y Accesos; ⌘K 6 grupos; rutas visibles 200; 13 rutas ocultas → UI 404 «Esta ruta no existe»; Clientes solo tab Resumen; deep-link comercial → Resumen; capturas desktop+móvil | 0 | 21 URLs + detalle | — | HTTP 200 con UI 404 por streaming (`loading.tsx`), ver §14 |
| **Smoke E2E Blog (§10, nuevo):** `smoke_blog.mjs` — crear→editar→autosave→publicar→verificar público→borrador 404→programado 404→categoría | login OK; publicado 200 con canonical/JSON-LD/FAQ/Maps/tags correctos; sitemap con la entrada; borrador y programado 404 | 0 | evidencia en `smoke-blog-report.json` | — | `<iframe>` de Maps sin contenido real por falta de red del sandbox hacia `google.com` (§10/§14), no CSP ni código |
| **Verificación de consola CSP (§10, nuevo):** `csp_check.mjs` sobre el artículo con Maps embebido | 0 errores de CSP en consola; `frame-src` incluye `www.google.com` | — | — | — | — |

Snapshots: no hay snapshots en la suite; ninguno se actualizó. `nav-integrity.test.ts`, `ClientWorkspace.test.tsx` y `public-post.test.ts` se **reescribieron/extendieron con revisión explícita** (cada expectativa corresponde a una decisión de la orden o de Miguel), no copiando la salida.

## 13. DOCUMENTACIÓN Y NEUROPIXEL

- Docs del repo actualizados/nuevos: `docs/dashboard-modules.md`, `docs/publicaciones-token-redes.md`, `docs/adr/ADR-0054-registro-central-de-modulos-y-limpieza-del-dashboard.md` (**propuesta, sin firma**; sección Blog+CSP actualizada tras la ejecución; numeración libre verificada: último = ADR-0053), `docs/pr/WO-2026-00088.md` (este), `docs/pr/WO-2026-00088-blog-matriz.md` (§6 Ejecución añadido), `docs/pr/WO-2026-00088-neuropixel-patch.md` (actualizado con el Blog ejecutado). `README.md` no se tocó (describe módulos ya obsoletos como `/dashboard`, `/herramientas`, `/crypto-intel`: su actualización excede esta orden; se anota en §14).
- NeuroPIXEL: **no se escribió en el vault** salvo el check-in/check-out de `09_SEGUIMIENTO/PixelTEC OS.md`. El contenido exacto para ficha, `referencia-tecnica.md`, `05_CAPACIDADES/nextjs.md` y `07_DECISIONES/ADR-0054` está preparado como parche, con el Blog ya reflejado como ejecutado. Estado real de sincronización: **pendiente de que Miguel apruebe `adr-sign`/`merge-to-main` y el SG aplique el parche**.
- Contexto consultado: ficha PixelTEC OS, ADR-0030, ADR-0035, ADR-0039, ADR-0001, ADR-0022, ADR-0028 (egress, para el wizard IA), ADR-0053 (formato), protocolo SG↔Worker, reglas vivas de Ingeniería (ING-001/002/003), `docs/pr/WO-2026-00055.md` (precedente), decisiones D-C/D-C-bis/CSP-Maps de Miguel registradas en el WO.

## 14. RIESGOS O BLOQUEOS (hechos verificables)

1. **HTTP 200 con UI 404 en rutas ocultas** (Playwright: `status 200`, marcador «Esta ruta no existe» presente en las 13 rutas ocultas del dashboard). Causa: `src/app/(admin)/loading.tsx` habilita streaming y Next envía cabeceras antes de `notFound()`. Seguridad no afectada (sesión exigida por middleware). Alternativa si se quiere código explícito: `redirect("/hoy")` (307) en `route-guard.ts`.
2. **Desbordamiento horizontal preexistente en móvil** (viewport 390): `main.clientWidth = 848` también en `/cobros` y `/usuarios` (archivos sin cambios) → no lo introduce este WO; visible en las capturas móviles. Posible fix de una línea (`min-w-0` en la columna flex del shell) queda **fuera de alcance** hasta orden.
3. **Embed de Google Maps del Blog sin verificación visual real**: la excepción de CSP quedó verificada (0 errores de consola, `frame-src` correcto), pero el `<iframe>` no pintó contenido porque este entorno de desarrollo no tiene salida de red hacia `google.com` (confirmado con `curl` directo: HTTP 400 también fuera del navegador). Recomendación: repetir la prueba visual desde un entorno con salida real antes del veredicto visual final de Miguel sobre el Blog.
4. **Pestaña «Snippets» de Encino no portada**: depende de su módulo SEO Control Center, que no existe en PixelTEC OS. Declarado en la matriz §2 como no implementado por dependencia ausente, no inventado.
5. **Migración `0043` (Blog) solo en BD de dev**: aplicarla en producción requiere el gate `merge-to-main`/deploy de Miguel, con el mismo procedimiento gobernado de `0042` (documentado en la cabecera del archivo).
6. `growth_social_accounts.access_token` en texto plano y sin renovación automática (documentado en §9; no corregido — fuera de alcance, sin relación con el Blog).
7. `README.md` desactualizado (módulos que ya no existen) — no se corrigió para no ampliar alcance.
8. Servidor `next dev` en `localhost:9002` (worktree) queda **encendido a propósito** para el veredicto visual de Miguel (declarado en Notas del doc de seguimiento); mock de PixelBot en :3011 pertenece al WO-2026-00047. La BD de dev conserva las entradas y la categoría del smoke del Blog como evidencia visual (§11).
9. **Datos de demo en BD de dev**: `Cliente Demo WO-88`, 3 entradas de Blog de prueba y la categoría «Automatización WO-88» — creados exclusivamente para las capturas del veredicto visual; no son datos de producción y pueden borrarse tras la revisión de Miguel.

## 15. REACTIVACIÓN

Para volver a mostrar u ocultar cualquier módulo (procedimiento completo en `docs/dashboard-modules.md`); el Blog nuevo ya está `active` — su fila es para **ocultarlo** si Miguel lo pidiera:

| Módulo | Cambio | Pruebas |
|---|---|---|
| Trabajo/Proyectos (y Definición, PixelForge) | `registry.ts` → `proyectos` (`definicion`, `pixelforge`): `state: "active"` | `npm test -- src/lib/modules src/components/nav` (ajustar `EXPECTED_STATES` y las etiquetas L1/L2 esperadas con revisión) · reaparecen solos: pill «Trabajo», tabs, quick action «Proyectos», KPIs «Proyectos activos/Tareas abiertas/Sesiones», gráfica y panel de Inicio; en Clientes, `client-workspace.ts` → `proyectos` depende del módulo (además `state: "active"` en la sección) |
| Marketing / Contenido / Campañas / Calendario / Publicaciones / Configuración de marca | mismo cambio en cada id (el hub `marketing` puede quedar oculto con hijos activos: el árbol se sirve) | idem + checklist de Publicaciones en `docs/publicaciones-token-redes.md` §16 |
| Infraestructura (`/vps`) | `infraestructura: active` | idem; vuelve la quick action «Infraestructura» de Inicio |
| Plantillas (`/ia-factory`), Archivo documental (`/documentos`) | `plantillas`/`documentos: active` | idem |
| Blog anterior (`/blog-admin`) | `blog-legacy: active` (o `legacy` → `active` y decidir `supersededBy`) | idem; la nav-integrity exige que no convivan dos entradas «Blog» ambiguas |
| Secciones de Clientes (Proyectos, Comercial, Documentos, Portal) | `client-workspace.ts`: `state: "active"` (+ módulo dependiente activo) | `npm test -- src/components/crm` (revisar `ClientWorkspace.test.tsx`) |
| Etiqueta «Hoy» | `NAV_AREA_LABELS.hoy` y el item `/hoy` en `command-palette-items.ts` | `nav-integrity.test.ts` |
| Blog (`/blog-cms`) | `registry.ts` → `blog: hidden` (o dejar `routes: []` para «declarado sin rutas», el estado previo a esta reasignación) | `nav-integrity.test.ts` + `registry.test.ts` (revisar `EXPECTED_STATES` y la posición 5 de la nav) |
| Blog anterior (`/blog-admin`) — legacy, comparte tabla con el Blog nuevo | `blog-legacy: active` (o `legacy` → `active`, decidir `supersededBy`) | idem; la nav-integrity exige que no convivan dos entradas «Blog» ambiguas; los posts creados en `/blog-cms` seguirían siendo editables desde `/blog-admin` porque es la misma tabla |

Los `layout.tsx` con guard **no se tocan** al reactivar/ocultar. Rollback completo del WO: revertir los commits locales de la rama; la única migración con estado en BD (`0043`, Blog) es aditiva y solo vive en dev — su rollback documentado está en la cabecera de `drizzle/0043_blog_encino_parity.sql` y no se necesita para revertir código, solo si además se quiere deshacer el esquema en esa base.
