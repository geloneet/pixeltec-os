# WO-2026-00186 — Limpieza del dashboard: notificaciones automáticas y funciones muertas

Entidad: Pixeltec.mx · Base: `main` `64a50b4` · Rama: `feature/limpieza-notificaciones` · Worktree: `wt-limpieza`.

## Lote 1 — ejecutado (orden literal de Miguel: «las notificaciones de actividades semanales o diarias elimínalas»)

Evidencia previa `[Verificado en infraestructura]`: crontab de `ubuntu` en el VPS disparaba a diario `GET /api/notifications/daily` (15:00 UTC) y `GET /api/notifications/charges` (14:00 UTC); nginx registró 7 hits 200 de cada uno en los últimos 7 días. `daily` recorría todos los usuarios, mandaba un WhatsApp de «Resumen diario» al número por defecto y creaba la notificación en la campana; `charges` avisaba de cobros recurrentes del CRM legacy (WhatsApp + correo + campana). No existe nada semanal en el código (`grep weekly|semanal|digest` solo da `sitemap`, el widget `WeeklyActivity` de Hoy y `crypto.digest`).

Hecho (`c36c6ad`): eliminadas `src/app/api/notifications/daily/route.ts`, `charges/route.ts` y `charges/route.test.ts`; `cron-guard.test.ts` pasa de 7 a 5 rutas; README y comentario de `CRMContextCore` actualizados. Verify: tsc 0 · ESLint 0 · vitest 138 archivos / 1882 pass / 1 skipped · `next build` exit 0. Crontab del VPS: las dos líneas retiradas con respaldo `~/backups/crontab-ubuntu-20260902T222006Z.bak`; queda solo `/api/cron/recurring-charges` (finanzas, intacto).

## Inventario de lo que ya no funciona o está muerto (Explore, solo lectura, `64a50b4`)

| # | Hallazgo | Evidencia | Propuesta |
|---|---|---|---|
| A1 | `/api/notifications/send` y `/test`: endpoints manuales de WhatsApp al número por defecto, sin consumidores | grep: solo `cron-guard.test.ts` y comentario en `sender.ts` | Borrar (lote 2) |
| A2 | `/api/notifications/billing-charges` (C6, ADR-0040): **ningún cron lo llama** en el VPS | `crontab -l` solo tiene `recurring-charges` | Miguel decide: programarlo o borrarlo |
| A3 | Filas «Resumen diario» acumuladas en la tabla `notifications` (campana) | `createNotification` desde `daily` | Miguel decide: limpiar en BD prod (una sentencia) o dejar que se descarten |
| B | Módulo VPS: `/api/vps/*` (10 rutas), `lib/vps-swr.ts`, `lib/vpsClient.ts`, `components/crm/ServerView.tsx` (sin importadores), poller `/api/vps/status` en el shell, env `VPS_API_URL/SECRET` | la página `/vps` se borró en WO-88; la API sigue viva sin UI | Borrar (lote 2) |
| C | Egress de IA huérfano: `lib/ai/openai-egress.ts` (0 importadores), `lib/ai/image-egress.ts` (solo un test), placeholder `OPENAI_API_KEY` | grep | Borrar (lote 2) |
| D | Restos de módulos borrados: `lib/db/repos/growth.ts`, `types/growth/*`, `app/(embed)/layout.tsx` sin páginas hijas | grep | Borrar (lote 2) |
| E | 19 tablas en `schema.ts` sin uso: `pixelforge*` (10), `definition*` (4), `clientPortalProjects`, `portalRequests`, `portalSecurityEvents`, `legacyTasks`, `growthJobs` | grep de exports | Lote 3 aparte: migración DROP + backup de BD prod; Miguel decide |
| F | Dependencias sin importar: `date-fns-tz`, `node-cron`, `@types/node-cron`, `patch-package`, `rrule`, `ws` | grep en todo el repo | Borrar (lote 2) |
| G | `/api/health*` sin caller en src | probable monitor externo | Conservar |

Fuera de alcance por diseño: `/whatsapp`, PixelBot, `/api/cron/recurring-charges`, portal de clientes (`/portal`, `/p`, `/c`), `smilemoreqa`, Unsplash y Anthropic (activos).

## Lote 3 (código y migración) — ejecutado

Retira del esquema Drizzle las 21 tablas que ningún código del repo usa y los enums que solo ellas usaban. Verificación previa, tabla por tabla: `grep -rnw <const> src` → cero referencias fuera de `src/lib/db/schema.ts`. Única mención residual: un comentario en `src/lib/blog/versions.ts` («Patrón calcado de pixelforgePageVersions…»), prosa histórica que se conserva.

**Tablas retiradas (21)** — nombre TS → nombre SQL:

| Bloque | Tablas |
|---|---|
| Portal legacy (Firestore) | `clientPortalProjects` → `client_portal_projects`, `portalRequests` → `portal_requests`, `portalSecurityEvents` → `portal_security_events` |
| Growth (módulo retirado) | `growthJobs` → `growth_jobs`, `growthSocialAccounts` → `growth_social_accounts` |
| Legacy Firestore | `legacyTasks` → `legacy_tasks` |
| Definición de Proyecto | `projectDefinitions` → `project_definitions`, `definitionStations` → `definition_stations`, `definitionMessages` → `definition_messages`, `definitionEvents` → `definition_events` |
| PixelForge (F1, sin UI) | `pixelforgeProjects`, `pixelforgeContextSources`, `pixelforgeArtifacts`, `pixelforgeEvents`, `pixelforgeAssets`, `pixelforgeAiRuns`, `pixelforgeVisualReferences`, `pixelforgeCreativeDirections`, `pixelforgePageVersions`, `pixelforgeQaRuns`, `pixelforgeQaFindings` → `pixelforge_*` homónimas |

Con ellas se fueron sus `relations(...)`, sus `export type … $inferSelect/$inferInsert` y **20 `pgEnum`**: `social_account_status`; `definition_station`, `definition_status`, `definition_station_status`, `definition_message_role`, `definition_event_type`; los 14 `pixelforge_*` (`station`, `status`, `artifact_kind`, `artifact_status`, `source_type`, `asset_kind`, `run_status`, `reference_coverage`, `reference_kind`, `direction_status`, `qa_run_status`, `qa_verdict`, `qa_severity`, `qa_browser_status`).

**Enums conservados a propósito** (los usan tablas vivas, verificado con grep): `organization_plan` (`growth_credits`), `credit_transaction_type` (`growth_credit_ledger`), `campaign_status` y `social_platform` (`growth_campaigns`), `post_status` (`growth_posts`). Ninguna relación de tabla viva (`clientsRelations`, `projectsRelations`, `toolsRelations`) apuntaba a las tablas retiradas: no hizo falta editar `one(...)`/`many(...)` de tablas vivas.

`src/lib/db/schema.ts` pasa de 2996 a 1961 líneas (−1035). Verify: `npx tsc --noEmit` 0 errores · `npm run lint` 0 issues · `npm test` 135 archivos / 1838 pass / 1 skipped (idéntico a la base).

### Migración `drizzle/0050_drop_dead_tables_wo186.sql`

Escrita **a mano** (no `drizzle-kit generate`), destructiva: `DROP TABLE IF EXISTS … CASCADE` para las 21 en orden hijos → padres, y `DROP TYPE IF EXISTS …` para los 20 enums. sha256: `7738039df827b6dd8e15b5756a6553f10e14060bd6ffed7ec438e737f370670d`.

**Dev.** Basta con `docker exec -i pixeltec-os-db psql -U pixeltec_os -d pixeltec_os < drizzle/0050_drop_dead_tables_wo186.sql` contra la BD local; el código nuevo ya no menciona esas tablas, así que puede aplicarse antes o después de levantar la app.

**Prod (pixeltec-os-db, 127.0.0.1:5437, user/db `pixeltec_os`).**
1. **Respaldo previo obligatorio** — sin él no se aplica: `docker exec pixeltec-os-db pg_dump -U pixeltec_os -d pixeltec_os -t <cada una de las 21 tablas> > ~/backups/drop-dead-tables-$(date -u +%Y%m%dT%H%M%SZ).sql` (el comando completo con los 21 `-t` está en el encabezado del .sql). Verificar que el volcado pesa > 0 y trae los 21 `CREATE TABLE`.
2. Aplicar: `docker exec -i pixeltec-os-db psql -U pixeltec_os -d pixeltec_os < drizzle/0050_drop_dead_tables_wo186.sql`.
3. Fila de control en `drizzle.__drizzle_migrations` con el hash sha256 de arriba y `created_at` = epoch en ms de la aplicación.
4. Verificar: `SELECT count(*) FROM information_schema.tables WHERE table_schema='public' AND table_name IN (…las 21…)` → 0, y que /clientes, /proyectos, /finanzas y /blog responden.
5. **Rollback:** no hay rollback SQL. Se restaura el volcado del paso 1 y se despliega la imagen anterior.

### Drift preexistente del journal (ajeno a este WO)

`drizzle/` contiene archivos hasta `0049_projects_trabajo_fields.sql`, pero `drizzle/meta/_journal.json` termina en `idx: 43` (`0043_blog_encino_parity`): **0044–0049 no están registrados** en el journal ni tienen snapshot. Es un drift que ya existía antes de este WO. Por eso 0050 se escribió a mano y **no** se tocaron `_journal.json` ni `meta/*_snapshot.json`: regenerar con `drizzle-kit generate` habría intentado reconciliar seis migraciones ajenas y habría reescrito el estado. La aplicación en prod se registra manualmente (paso 3), igual que 0038–0049 según `infraestructura.md`.
