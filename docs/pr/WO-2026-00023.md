# WO-2026-00023 — Preparación de PR · deploy gobernado · smoke real (SIN publicar)

Preparado por Worker PixelTEC OS [67518b] el 2026-08-24 (TASK-PREP, opción C). **Este documento no ejecuta nada**: lo publica el Supervisor General con el gate de Miguel. Rama: `feature/whatsapp-template-nueva-respuesta` @ `ccf68da` (base `main` = `c35fbdb`). Cambios de código: WO-2026-00019 (commits `058fb79`, `ccf68da`). Etiquetas: `[Verificado en código]` · `[Documentado en NeuroPIXEL]` · `[Propuesta]`.

---

## Artefacto 1 — Cuerpo del PR

**Título:** `feat(whatsapp): plantilla nueva_respuesta_cuestionario + ruta pública /respuestas/[id] (WO-2026-00019)`

**Comando (lo ejecuta el SG/Miguel, no el Worker):**

```bash
cd /Users/pixeltec/Desktop/PixelTECDEV/pixeltec-os
git push -u origin feature/whatsapp-template-nueva-respuesta
gh pr create --base main --head feature/whatsapp-template-nueva-respuesta \
  --title "feat(whatsapp): plantilla nueva_respuesta_cuestionario + ruta pública /respuestas/[id] (WO-2026-00019)" \
  --body-file docs/pr/WO-2026-00023.pr-body.md   # o pegar el bloque de abajo
# Merge SIN squash (gate de Miguel: «sin squash de evidencia»):
gh pr merge <n> --merge --delete-branch=false
git fetch origin && git rev-parse origin/main      # ← SHA de 40 hex del merge commit: es el que se despliega
```

**Body (pegar tal cual):**

```markdown
## Qué cambia
El aviso al equipo por cada respuesta del cuestionario `/smilemoreqa` deja de salir como **texto libre** (rechazado por Meta fuera de la ventana de 24 h, error `131047`) y pasa a la **plantilla UTILITY aprobada `nueva_respuesta_cuestionario` (`es_MX`)**, con botón «Ver respuesta» → `https://pixeltec.mx/respuestas/{id}`.

- `src/lib/whatsapp/sender.ts`: transporte único (env, egress-guard fail-closed, timeout 15 s, `redirect: manual`, logs enmascarados) compartido por `sendWhatsApp()` (texto libre, **intacto**) y la nueva `sendWhatsAppTemplate({to?, formName, clientName, reference, responseId})`. `MetaWhatsAppError` tipado (`code/subcode/status/fbtraceId`) sin cambiar mensajes.
- `src/lib/whatsapp/templates.ts`: builder **puro** del payload (body `[formName, clientName, reference]`, botón `url` index 0 con **solo el sufijo** `responseId`; Meta antepone la base). Sanea variables (sin saltos de línea / >4 espacios / vacías).
- `src/lib/whatsapp/nueva-respuesta.ts`: envía y deja **evidencia propia** (D-21a) en `system_alerts` (`source='whatsapp_template'`: `message_id`, plantilla, destinatario enmascarado, `sentAt`, `result accepted|failed`, código de Meta). Nunca lanza. **No hay webhook nuevo**: PixelBot sigue siendo dueño de los statuses.
- `src/app/actions.ts` `submitSmilemoreQa`: datos semánticos (`QUESTIONNAIRE_META.title/.client`, sucursal → fallback nombre, `responseId`). Persistencia sigue siendo ANTES del aviso.
- **Ruta pública `/respuestas/[id]`** (D-22): `src/lib/respuestas/resolver.ts` (Smile More existente → redirect `/smilemore-respuestas/[id]`, que conserva su auth admin; id no reconocido ⇒ **404**). Extensible a Encino/otros sin cambiar URL ni plantilla. Sin tabla nueva.
- 0 valores de muestra de Meta («Smile More Dental», «Guadalajara») en `src/lib/whatsapp` / `src/lib/respuestas`: `no-hardcode.test.ts` lo vigila.

Sin migraciones. Sin env nuevas (`WHATSAPP_ACCESS_TOKEN`, `WHATSAPP_PHONE_NUMBER_ID`, `WHATSAPP_DEFAULT_TO`, `WHATSAPP_API_VERSION?`, `EGRESS_WHATSAPP_*` ya cargadas en el contrato E0).

## Gates (rama @ ccf68da)
- `tsc --noEmit`: 0 errores en `src/` (1 preexistente en `.next/types` generado obsoleto de pixelforge, igual que en #109).
- `npm test`: 212 archivos / 2728 tests verdes (SG: 155/155 · 2728/2728 verificados de forma independiente).
- ESLint: 0 issues en archivos tocados. Grep de muestras hardcodeadas: 0.
- Sin envío real de WhatsApp en tests (fetch stubbeado).

## Riesgos
1. `/respuestas/[id]` hace un lookup anónimo en DB por uuid v4 (enumeración impracticable; **sin rate limit**, fuera de scope — Miguel decide si se añade). No está en `robots.txt` como disallow (no es ruta admin); el destino sí.
2. Primer envío real de la plantilla aún no probado: si Meta responde `132xxx` (plantilla/parámetros) la evidencia quedará `result: failed` con el código, y el envío del cuestionario NO se pierde (persistencia previa).
3. Meta acepta `index: 0` numérico; si rechazara, el fallback es `"0"` (cambio de 1 línea en `templates.ts`).

## Checklist de smoke (post-deploy, ver docs/pr/WO-2026-00023.md §2-3)
- [ ] `/`, `/login`, `/smilemoreqa` → 200 · `/respuestas/<uuid inexistente>` y `/respuestas/abc` → 404 · `/smilemore-respuestas` → 307 login
- [ ] 1 envío real desde `/smilemoreqa` con datos «PRUEBA SG WO-23» → `system_alerts` con `result=accepted` + `messageId` (wamid) · Miguel confirma recepción en su WhatsApp
- [ ] `/respuestas/<id de prueba>` → 307 → `/smilemore-respuestas/<id>` → 307 → `/login?redirect=…`
- [ ] Fila de prueba borrada (id documentado) · docs de seguimiento/infra actualizados

Refs: WO-2026-00019 (implementación) · WO-2026-00023 (publicación) · D-21 (a) evidencia propia sin webhook · D-22 ruta pública genérica · brief `04_PRODUCTOS/PixelTEC OS/brief-whatsapp-plantilla-nueva-respuesta.md` · ficha `05_CAPACIDADES/whatsapp-cloud-api.md`.

🤖 Generated with [Claude Code](https://claude.com/claude-code)
```

---

## Artefacto 2 — Plan de deploy / rollback (vía gobernada, SECURITY-002)

`[Documentado en NeuroPIXEL · 01_CONTEXT/infraestructura.md fila pixeltec.mx + 02_PLAYBOOKS/deploy-a-produccion.md §Predeploy PixelTEC OS]` · `[Verificado en código · scripts/deploy/production-deploy.sh, deploy-pixeltec-os-wrapper.sh]`

**Único camino autorizado:** `/usr/local/sbin/deploy-pixeltec-os --sha <40-hex> [--check-only]` como `ubuntu` en el VPS. Exige que el SHA exista en `origin` y sea **ancestro de `origin/main`** ⇒ el merge del PR va ANTES. No hay migraciones en este cambio ⇒ no correr `db:migrate`. No se necesitan capabilities extra (`--require-*`): el canal WhatsApp ya está `live` en el contrato E0; Meta sigue `disabled`.

```bash
# 0) Local (SG/Miguel) — tras el merge:
git fetch origin && DEPLOY_SHA=$(git rev-parse origin/main) && echo "$DEPLOY_SHA"
git merge-base --is-ancestor ccf68da "$DEPLOY_SHA" && echo "ccf68da incluido"

# 1) VPS
ssh ubuntu@198.100.155.231

# 2) Rollback previo identificado ANTES de tocar nada (leer el archivo, no asumir)
cat /home/ubuntu/pixeltec-os/.deploy-active-sha            # ← ROLLBACK_SHA (debe ser c35fbdb2853a7fe6b5bdbda3ce163400c2e66859 según infraestructura.md)
docker image inspect pixeltec-os-app:$(cat /home/ubuntu/pixeltec-os/.deploy-active-sha) --format '{{.Id}}'   # imagen de rollback existe
df -h / | tail -1                                           # disco: cada deploy suma ~2 GB (DISK-001)

# 3) Check-only (todas las validaciones, sin build ni activación)
deploy-pixeltec-os --sha <DEPLOY_SHA> --check-only
#    esperado: "CHECK-ONLY OK sha=… — sin build, sin activación, sin cambios"; rc=0

# 4) Deploy real
deploy-pixeltec-os --sha <DEPLOY_SHA>
#    esperado: [1/9]…[9/9], "health: sitio=301 login=301" (301 = HTTP→HTTPS vía nginx, es el valor histórico),
#    "DEPLOY OK sha=… rollback=no", rc=0. Evidencia: /home/ubuntu/deploy-logs/pixeltec-os-<UTC>-<sha12>-deploy.log
echo "rc=$?"; ls -t /home/ubuntu/deploy-logs | head -2
```

**Smoke post-deploy (desde la Mac o el VPS; códigos esperados):**

```bash
for p in / /login /smilemoreqa; do printf '%-22s %s\n' "$p" "$(curl -s -o /dev/null -w '%{http_code}' https://pixeltec.mx$p)"; done
#   /            200      /login  200      /smilemoreqa  200
curl -s -o /dev/null -w '%{http_code}\n' https://pixeltec.mx/respuestas/00000000-0000-4000-8000-000000000000   # 404 (uuid válido, inexistente)
curl -s -o /dev/null -w '%{http_code}\n' https://pixeltec.mx/respuestas/abc                                    # 404 (id malformado)
curl -s -o /dev/null -w '%{http_code} %{redirect_url}\n' https://pixeltec.mx/smilemore-respuestas              # 307 → /login?redirect=/smilemore-respuestas
curl -sI https://pixeltec.mx/smilemoreqa | grep -i x-robots-tag                                                  # noindex,nofollow (sin cambio)
```

**Logs a mirar (primeros 10 min):**

```bash
docker logs pixeltec-os --since 10m 2>&1 | grep -iE "error|EgressBlockedError|ECONNREFUSED" | grep -v "kaboom" | head
docker logs pixeltec-os --since 10m 2>&1 | grep -E "\[whatsapp\]"        # solo tras el smoke real: "sending to ****", "sent {messageId, to}"
docker inspect pixeltec-os --format 'status={{.State.Status}} restarts={{.RestartCount}}'
```

**Rollback (solo si health falla o el smoke real devuelve error de Meta ⇒ BLOCKED, sin reintentos):**

- Automático: el motor ya restaura `.deploy-active-sha` ante `HEALTH FAIL` (`DEPLOY FAILED … rollback=<prev>`).
- Manual gobernado (preferido): `deploy-pixeltec-os --sha <ROLLBACK_SHA>` (la imagen `pixeltec-os-app:<ROLLBACK_SHA>` existe; el motor la reconstruye desde la release inmutable y vuelve a pasar health).
- Emergencia (equivalente al paso de rollback del motor, sin build):
  ```bash
  cd /home/ubuntu && PIXELTEC_OS_IMAGE_TAG=<ROLLBACK_SHA> env PIXELTEC_OS_ENV_FILE=/home/ubuntu/pixeltec-os/.env.production \
    docker compose -p pixeltec-os -f /home/ubuntu/pixeltec-os-releases/<ROLLBACK_SHA>/docker-compose.yml \
    --env-file /home/ubuntu/pixeltec-os/.env.production up -d --no-build --no-deps app && docker exec pixeltec-nginx nginx -s reload
  ```
  Nota: el error de Meta en el smoke **no** exige rollback técnico (el binario nuevo no rompe nada: persiste igual y solo falla el aviso), pero la regla del WO lo pide ⇒ rollback + BLOCKED con el código literal.

---

## Artefacto 3 — Plan del smoke real (UN solo envío)

**Precondiciones:** deploy OK; `WHATSAPP_DEFAULT_TO` en `.env.production` = número de Miguel (**no se cambia**; ni siquiera se imprime: `grep -c '^WHATSAPP_DEFAULT_TO=' /home/ubuntu/pixeltec-os/.env.production` → 1). Anotar hora exacta UTC y local antes de enviar.

**Datos de prueba en `https://pixeltec.mx/smilemoreqa`** (navegador, formulario real):

| Campo | Valor |
|---|---|
| Nombre | `PRUEBA SG WO-23` |
| Puesto | `Dirección / Administración` |
| Sucursal | `Guadalajara` → será `{{3}}` de la plantilla (reference) |
| Uso | `Ocasional` |
| Resto | mínimo obligatorio; texto libre `PRUEBA SG WO-23 — ignorar` |

**Mensaje esperado en el WhatsApp de Miguel:** «Nueva respuesta recibida» / «Se recibió una nueva respuesta del cuestionario **Corrección y Adaptación de Sistema** para **Smile More**.» / «Referencia: **Guadalajara**» / «Puedes revisar los detalles en PixelTEC OS.» + botón **Ver respuesta** → `https://pixeltec.mx/respuestas/<id>`.

**Evidencia (D-21a) — system_alerts (DB real `pixeltec-os-db`, user/db `pixeltec_os`):**

```bash
docker exec -i pixeltec-os-db psql -U pixeltec_os -d pixeltec_os -At -c "
select created_at, severity,
       context->>'result'      as result,
       context->>'messageId'   as message_id,
       context->>'template'    as template,
       context->>'to'          as to_masked,
       context->>'sentAt'      as sent_at,
       context->'context'->>'responseId' as response_id,
       context->'error'        as error
from system_alerts
where source = 'whatsapp_template'
order by created_at desc limit 3;"
```

Esperado: 1 fila nueva con `result=accepted`, `message_id=wamid.…`, `template=nueva_respuesta_cuestionario`, `to_masked=+52*********NNNN`, `response_id=<uuid>`. Si `result=failed`: `error->>'code'` es el código literal de Meta (p. ej. `132018` parámetros, `132001` plantilla/idioma, `131047` ventana, `131030` allowlist) ⇒ **BLOCKED con ese código, sin reintentar**. Alternativa/confirmación en logs: `docker logs pixeltec-os --since 15m 2>&1 | grep -E "\[whatsapp\] (sending|sent|template)"`.

**Id de la respuesta de prueba (si hiciera falta sin system_alerts):**

```sql
select id, respondent_name, branch, created_at
from smilemore_qa_responses
where respondent_name like 'PRUEBA SG WO-23%'
order by created_at desc;
```

**Verificación de `/respuestas/<id>`** (sin credenciales; basta la cadena de redirects):

```bash
curl -s -o /dev/null -w '%{http_code} %{redirect_url}\n' https://pixeltec.mx/respuestas/<id>
#   307 https://pixeltec.mx/smilemore-respuestas/<id>
curl -s -o /dev/null -w '%{http_code} %{redirect_url}\n' https://pixeltec.mx/smilemore-respuestas/<id>
#   307 https://pixeltec.mx/login?redirect=%2Fsmilemore-respuestas%2F<id>
curl -sL -o /dev/null -w '%{http_code} %{url_effective}\n' https://pixeltec.mx/respuestas/<id>
#   200 https://pixeltec.mx/login?redirect=…   (final sin sesión)
```
Con sesión admin de Miguel (opcional, desde su navegador): tocar el botón del WhatsApp → login → detalle de la respuesta «PRUEBA SG WO-23». Eso cierra el veredicto visual (ING-001) de la ruta nueva.

**Borrado de la fila de prueba (parametrizado; igual que el 2026-08-19):**

```bash
ID='<uuid de la respuesta de prueba>'
docker exec -i pixeltec-os-db psql -U pixeltec_os -d pixeltec_os -v ON_ERROR_STOP=1 -c "
begin;
select id, respondent_name from smilemore_qa_responses where id = '$ID';   -- debe mostrar PRUEBA SG WO-23
delete from smilemore_qa_responses where id = '$ID' and respondent_name like 'PRUEBA SG WO-23%' returning id;
commit;"
docker exec -i pixeltec-os-db psql -U pixeltec_os -d pixeltec_os -At -c "select count(*) from smilemore_qa_responses where id = '$ID';"   # 0
```
El doble predicado (`id` + `respondent_name like 'PRUEBA SG WO-23%'`) hace imposible borrar una respuesta real por un id mal pegado. **Las filas de `system_alerts` se conservan** (son la evidencia del envío; no contienen PII: número enmascarado). Documentar el `id` borrado en el RESULT.

**Docs a actualizar tras el smoke (anti-drift):** `09_SEGUIMIENTO/PixelTEC OS.md` (check-out con deploy sha, rollback, message_id, hora, id borrado) · `04_PRODUCTOS/PixelTEC OS/` ficha (deploy vigente) · `01_CONTEXT/infraestructura.md` fila `pixeltec.mx` (deploy vigente = merge sha, rollback = `c35fbdb…`, log del deploy) · `03_CLIENTES/Smile More.md` / `09_SEGUIMIENTO/Smile More.md` (aviso ahora por plantilla, botón directo a la respuesta) · `05_CAPACIDADES/whatsapp-cloud-api.md` (primer envío real: fecha + wamid enmascarado).

---

`[Propuesta]` Añadir `Disallow: /respuestas` en `robots.txt` (ruta pública de resolución, no debe indexarse) — 1 línea, fuera de este WO.
