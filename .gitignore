node_modules/
.next/
.env
.env.local
pixeltec-source.tar.gz
nginx/ssl/
.env.production
.env.production.backup*

# Emergency backups created during ops work
*.bak.*

# TypeScript incremental build cache (artifact, no source)
tsconfig.tsbuildinfo
*.tsbuildinfo
.worktrees/

# Dev server output (not source)
dev.log
