node_modules/
.next/
.env
.env.local
pixeltec-source.tar.gz
nginx/ssl/
.env.production

# Emergency backups created during ops work
*.bak.*

# TypeScript incremental build cache (artifact, no source)
tsconfig.tsbuildinfo
*.tsbuildinfo
.worktrees/

# macOS
.DS_Store
**/.DS_Store

# Claude Code local settings (no compartir entre devs)
.claude/settings.local.json
