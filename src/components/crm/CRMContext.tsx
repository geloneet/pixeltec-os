// Barrel re-export — all imports now use CRMContextCore directly.
// Kept here so any old cached browser chunk reference resolves without error.
export { useCRM, CRMProvider } from "./CRMContextCore";
