/**
 * Clúster C — desarrollo de apps (WO-2026-00189).
 *
 * Keywords del plan §2: «desarrolladores de app», «desarrollo de app»,
 * «desarrolladores de apps» y «desarrollo de aplicaciones moviles», cada una
 * con su variante `-puerto-vallarta`. Hub: `ecosistemas-web`.
 *
 * VACÍO A PROPÓSITO: lo llena la parte 3/3 del WorkOrder. La infraestructura
 * (registro, componente, generador, sitemap, enlazado y test) ya está en pie,
 * así que basta con agregar entradas a este array — no hay que tocar nada más.
 */

import type { KeywordLanding } from './keyword-landings';

export const KEYWORD_LANDINGS_APPS: KeywordLanding[] = [];
