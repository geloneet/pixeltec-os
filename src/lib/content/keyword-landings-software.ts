/**
 * Clúster A — software a medida (WO-2026-00189).
 *
 * Keywords del plan §2: «empresas de desarrollo de software», «programador de
 * software», «sistemas a medida», «software a medida para empresas» y
 * «sistema personalizado para empresas», cada una con su variante
 * `-puerto-vallarta`. Hub: `ecosistemas-web`.
 *
 * VACÍO A PROPÓSITO: lo llena la parte 2/3 del WorkOrder. La infraestructura
 * (registro, componente, generador, sitemap, enlazado y test) ya está en pie,
 * así que basta con agregar entradas a este array — no hay que tocar nada más.
 */

import type { KeywordLanding } from './keyword-landings';

export const KEYWORD_LANDINGS_SOFTWARE: KeywordLanding[] = [];
